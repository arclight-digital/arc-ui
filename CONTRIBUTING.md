# Contributing to ARC UI

Thanks for your interest in contributing to ARC UI! This guide covers the basics for getting started.

## Getting Started

1. Fork the repository
2. Clone your fork and install dependencies (Node >= 24, pnpm >= 10):

```bash
git clone https://github.com/<your-username>/arc-ui.git
cd arc-ui
pnpm install
```

3. Start the docs site (the development playground):

```bash
pnpm dev
```

## Project Structure

```
packages/
  web-components/   # Lit web components (source of truth — the only package you edit)
  react/            # React wrappers      (GENERATED by @arclux/prism)
  vue/              # Vue wrappers        (GENERATED)
  svelte/           # Svelte wrappers     (GENERATED)
  angular/          # Angular wrappers    (GENERATED)
  solid/            # Solid wrappers      (GENERATED)
  preact/           # Preact wrappers     (GENERATED)
  html/             # Standalone CSS + HTML examples (GENERATED)
docs/               # Astro documentation site (arcui.dev)
shared/             # Design tokens: tokens.js (source), base.css (GENERATED)
scripts/            # The generate pipeline + static assertion checks
```

Never hand-edit generated files — `pnpm generate` recreates them all, and CI
fails any commit where the generated output doesn't match the source.

## Development Workflow

### Adding or modifying a component

All components live in `packages/web-components/src/`, organized by tier:

- `content/` — static display (Card, Avatar, Callout, …)
- `data/` — data display (Table, DataGrid, Badge, Timeline, …)
- `typography/` — text (Prose, CodeBlock, Markdown, …)
- `input/` — form controls (Button, Input, Combobox, DatePicker, …)
- `navigation/` — nav (Sidebar, Tabs, Menubar, Breadcrumb, …)
- `layout/` — structure (AppShell, Container, SplitPane, …)
- `feedback/` — messaging and overlays (Modal, Toast, Alert, Popover, …)
- `shared/` — mixins, controllers, and helper elements used across tiers

After any change, regenerate everything:

```bash
pnpm generate
```

This rebuilds tokens CSS, element registrations, all six framework wrapper
packages, the HTML/CSS package, `custom-elements.json`, the published type
declarations, editor integration data, and the README stats — and then runs
the static assertion checks (prop unions, enum fallbacks, wrapper slots,
child registrations, event conventions, wrapper typecheck). Commit the
regenerated output together with your source change.

### Testing

```bash
pnpm test          # web-test-runner + Chromium (packages/web-components/test/)
pnpm lint          # ESLint over hand-written source
pnpm audit:a11y    # axe-core audit of the built docs site (needs pnpm build:docs first)
```

All three run in CI. Tests favor cross-cutting invariants (font roles, enum
fallbacks, event conventions) over per-component snapshots — if you fix a
shipped bug, add a test that pins the invariant that broke.

### Conventions

- **Web components**: `arc-component-name` (kebab-case tag), `ArcComponentName` (PascalCase class)
- **React wrappers**: `ComponentName` (no Arc prefix)
- **Events**: `arc-event-name` with `bubbles: true, composed: true` (checked by `pnpm check:events`)
- **Child elements**: if a component renders another `arc-*` element, declare it
  with a `@requires` JSDoc tag so per-component imports register it (checked by
  `pnpm check:child-registrations`)
- **Styles**: all components import `tokenStyles` from `../shared-styles.js`
- **Tokens**: edit `shared/tokens.js` only; use `var(--token)` — never hardcode
  colors, spacing, or font families (font faces are role slots, see the
  typography docs)
- **JSDoc is load-bearing**: component JSDoc generates the docs API tables, the
  type declarations, and the editor data. An undocumented prop is an untyped prop.

### Pull Requests

- Create a feature branch from `main`
- Keep PRs focused — one feature or fix per PR
- Include a clear description of what changed and why
- If adding a new component, include a docs page in `docs/src/data/components/`

## AI / LLM Policy

ARC UI welcomes contributions that use AI and LLM tools as part of the development workflow. We recognize these tools can accelerate development and help contributors be more productive.

**Requirements:**

- **All AI-generated code must be reviewed and verified by a human before submitting a pull request.** The submitter is responsible for understanding every line of code in their PR, regardless of how it was produced.
- **PRs must pass all existing checks** — AI tools do not exempt contributions from quality standards.
- **Disclose significant AI usage** in your PR description if the majority of the code was AI-generated. A simple note like "Authored with AI assistance" is sufficient.

**Automated / agentic PRs:**

Pull requests opened by fully autonomous agents, bots, or agentic solutions (e.g., OpenClaw, Sweep, or similar services) without meaningful human review **will be automatically closed**. We value human judgment in the review process. If you use an agentic tool, you must review its output yourself and submit the PR from your own account with your own sign-off.

## Code of Conduct

Please read our [Code of Conduct](CODE_OF_CONDUCT.md) before participating. We're committed to providing a welcoming and inclusive experience for everyone.

## License

By contributing to ARC UI, you agree that your contributions will be licensed under the [MIT License](LICENSE).
