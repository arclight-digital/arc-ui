/**
 * Re-exports @lit/react createComponent.
 * Individual wrappers import @lit/react directly — this file exists
 * only for backwards compatibility with any external consumers.
 */
export { createComponent, type EventName } from '@lit/react';
