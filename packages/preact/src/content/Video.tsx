// Auto-generated by @arclux/prism — do not edit manually

import { h, type FunctionComponent } from 'preact';
import { useLayoutEffect, useRef } from 'preact/hooks';
import '@arclux/arc-ui/video';

export interface VideoProps {
  src?: string;
  poster?: string;
  label?: string;
  autoplay?: boolean;
  loop?: boolean;
  muted?: boolean;
  controls?: string;
  preload?: 'none' | 'metadata' | 'auto';
  onArcPlay?: (e: CustomEvent) => void;
  onArcPause?: (e: CustomEvent) => void;
  onArcEnded?: (e: CustomEvent) => void;
  class?: string;
  id?: string;
  style?: string;
  title?: string;
  role?: string;
  slot?: string;
  part?: string;
  exportparts?: string;
  dir?: string;
  lang?: string;
  translate?: string;
  accesskey?: string;
  enterkeyhint?: string;
  inputmode?: string;
  popover?: string;
  contenteditable?: boolean | string;
  tabindex?: number;
  hidden?: boolean;
  inert?: boolean;
  draggable?: boolean;
  spellcheck?: boolean;
  autofocus?: boolean;
  [key: `data-${string}`]: unknown;
  [key: `aria-${string}`]: unknown;
  [key: `on${string}`]: unknown;
}

export const Video: FunctionComponent<VideoProps> = ({ src, poster, label, autoplay, loop, muted, controls, preload, onArcPlay, onArcPause, onArcEnded, ...rest }) => {
  const ref = useRef<HTMLElement>(null);
  useLayoutEffect(() => {
    const el = ref.current;
    if (!el) return;
    const listeners: Array<[string, EventListener]> = [];
    if (onArcPlay) {
      const fn: EventListener = (e) => onArcPlay(e as CustomEvent);
      el.addEventListener('arc-play', fn);
      listeners.push(['arc-play', fn]);
    }
    if (onArcPause) {
      const fn: EventListener = (e) => onArcPause(e as CustomEvent);
      el.addEventListener('arc-pause', fn);
      listeners.push(['arc-pause', fn]);
    }
    if (onArcEnded) {
      const fn: EventListener = (e) => onArcEnded(e as CustomEvent);
      el.addEventListener('arc-ended', fn);
      listeners.push(['arc-ended', fn]);
    }
    return () => listeners.forEach(([name, fn]) => el.removeEventListener(name, fn));
  }, [onArcPlay, onArcPause, onArcEnded]);
  return h('arc-video', { ref, src, poster, label, autoplay, loop, muted, controls, preload, ...rest });
};
