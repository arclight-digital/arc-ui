// Auto-generated by @arclux/prism — do not edit manually

import { type FunctionComponent } from 'preact';
import '@arclux/arc-ui/carousel';

export interface CarouselProps {
  autoPlay?: boolean;
  interval?: number;
  loop?: boolean;
  showDots?: boolean;
  showArrows?: boolean;
  children?: preact.ComponentChildren;
  [key: string]: unknown;
}

export const Carousel: FunctionComponent<CarouselProps> = ({ autoPlay, interval, loop, showDots, showArrows, children, ...rest }) => (
  <arc-carousel autoPlay={autoPlay} interval={interval} loop={loop} showDots={showDots} showArrows={showArrows} {...rest}>
    {children}
  </arc-carousel>
);
