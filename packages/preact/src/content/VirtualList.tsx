// Auto-generated by @arclux/prism — do not edit manually

import { h, type FunctionComponent } from 'preact';
import { useLayoutEffect, useRef } from 'preact/hooks';
import '@arclux/arc-ui/virtual-list';

export interface VirtualListProps {
  items?: unknown[];
  renderItem?: Function;
  itemHeight?: number;
  overscan?: number;
  onArcRangeChange?: (e: CustomEvent) => void;
  children?: preact.ComponentChildren;
  class?: string;
  id?: string;
  style?: string;
  title?: string;
  role?: string;
  slot?: string;
  part?: string;
  exportparts?: string;
  dir?: string;
  lang?: string;
  translate?: string;
  accesskey?: string;
  enterkeyhint?: string;
  inputmode?: string;
  popover?: string;
  contenteditable?: boolean | string;
  tabindex?: number;
  hidden?: boolean;
  inert?: boolean;
  draggable?: boolean;
  spellcheck?: boolean;
  autofocus?: boolean;
  [key: `data-${string}`]: unknown;
  [key: `aria-${string}`]: unknown;
  [key: `on${string}`]: unknown;
}

export const VirtualList: FunctionComponent<VirtualListProps> = ({ items, renderItem, itemHeight, overscan, onArcRangeChange, children, ...rest }) => {
  const ref = useRef<HTMLElement>(null);
  useLayoutEffect(() => {
    const el = ref.current;
    if (!el) return;
    const listeners: Array<[string, EventListener]> = [];
    if (onArcRangeChange) {
      const fn: EventListener = (e) => onArcRangeChange(e as CustomEvent);
      el.addEventListener('arc-range-change', fn);
      listeners.push(['arc-range-change', fn]);
    }
    return () => listeners.forEach(([name, fn]) => el.removeEventListener(name, fn));
  }, [onArcRangeChange]);
  return h('arc-virtual-list', { ref, items, renderItem, itemHeight, overscan, ...rest }, children);
};
