// Auto-generated by @arclux/prism — do not edit manually

import { type FunctionComponent } from 'preact';
import { useLayoutEffect, useRef } from 'preact/hooks';
import '@arclux/arc-ui/tag';

export interface TagProps {
  variant?: 'primary' | 'secondary' | 'success' | 'warning' | 'danger';
  size?: 'sm' | 'lg';
  removable?: boolean;
  disabled?: boolean;
  color?: string;
  onArcRemove?: (e: CustomEvent) => void;
  children?: preact.ComponentChildren;
  [key: string]: unknown;
}

export const Tag: FunctionComponent<TagProps> = ({ variant, size, removable, disabled, color, onArcRemove, children, ...rest }) => {
  const ref = useRef<HTMLElement>(null);
  useLayoutEffect(() => {
    const el = ref.current;
    if (!el) return;
    const listeners: Array<[string, EventListener]> = [];
    if (onArcRemove) {
      const fn: EventListener = (e) => onArcRemove(e as CustomEvent);
      el.addEventListener('arc-remove', fn);
      listeners.push(['arc-remove', fn]);
    }
    return () => listeners.forEach(([name, fn]) => el.removeEventListener(name, fn));
  }, [onArcRemove]);
  return (
    <arc-tag ref={ref} variant={variant} size={size} removable={removable} disabled={disabled} color={color} {...rest}>
      {children}
    </arc-tag>
  );
};
