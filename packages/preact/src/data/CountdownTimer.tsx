// Auto-generated by @arclux/prism — do not edit manually

import { type FunctionComponent } from 'preact';
import { useLayoutEffect, useRef } from 'preact/hooks';
import '@arclux/arc-ui/countdown-timer';

export interface CountdownTimerProps {
  target?: string;
  label?: string;
  expired?: string;
  hideZeroSegments?: boolean;
  onArcExpired?: (e: CustomEvent) => void;
  children?: preact.ComponentChildren;
  [key: string]: unknown;
}

export const CountdownTimer: FunctionComponent<CountdownTimerProps> = ({ target, label, expired, hideZeroSegments, onArcExpired, children, ...rest }) => {
  const ref = useRef<HTMLElement>(null);
  useLayoutEffect(() => {
    const el = ref.current;
    if (!el) return;
    const listeners: Array<[string, EventListener]> = [];
    if (onArcExpired) {
      const fn: EventListener = (e) => onArcExpired(e as CustomEvent);
      el.addEventListener('arc-expired', fn);
      listeners.push(['arc-expired', fn]);
    }
    return () => listeners.forEach(([name, fn]) => el.removeEventListener(name, fn));
  }, [onArcExpired]);
  return (
    <arc-countdown-timer ref={ref} target={target} label={label} expired={expired} hideZeroSegments={hideZeroSegments} {...rest}>
      {children}
    </arc-countdown-timer>
  );
};
