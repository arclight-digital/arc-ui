// Auto-generated by @arclux/prism — do not edit manually

import { type FunctionComponent } from 'preact';
import { useLayoutEffect, useRef } from 'preact/hooks';
import '@arclux/arc-ui/chart';

export interface ChartProps {
  type?: string;
  series?: unknown[];
  labels?: unknown[];
  stacked?: boolean;
  hideLegend?: boolean;
  hideAxis?: boolean;
  height?: number;
  valueFormat?: string;
  currency?: string;
  onArcMarkClick?: (e: CustomEvent) => void;
  children?: preact.ComponentChildren;
  [key: string]: unknown;
}

export const Chart: FunctionComponent<ChartProps> = ({ type, series, labels, stacked, hideLegend, hideAxis, height, valueFormat, currency, onArcMarkClick, children, ...rest }) => {
  const ref = useRef<HTMLElement>(null);
  useLayoutEffect(() => {
    const el = ref.current;
    if (!el) return;
    const listeners: Array<[string, EventListener]> = [];
    if (onArcMarkClick) {
      const fn: EventListener = (e) => onArcMarkClick(e as CustomEvent);
      el.addEventListener('arc-mark-click', fn);
      listeners.push(['arc-mark-click', fn]);
    }
    return () => listeners.forEach(([name, fn]) => el.removeEventListener(name, fn));
  }, [onArcMarkClick]);
  return (
    <arc-chart ref={ref} type={type} series={series} labels={labels} stacked={stacked} hideLegend={hideLegend} hideAxis={hideAxis} height={height} valueFormat={valueFormat} currency={currency} {...rest}>
      {children}
    </arc-chart>
  );
};
