// Auto-generated by @arclux/prism — do not edit manually

import { type FunctionComponent } from 'preact';
import { useLayoutEffect, useRef } from 'preact/hooks';
import '@arclux/arc-ui/data-table';

export interface DataTableProps {
  rows?: Array<Record<string, any>>;
  sortable?: boolean;
  selectable?: boolean;
  sortColumn?: string;
  sortDirection?: 'asc' | 'desc';
  virtual?: boolean;
  rowHeight?: number;
  onArcSort?: (e: CustomEvent) => void;
  onArcSelectAll?: (e: CustomEvent) => void;
  onArcRowSelect?: (e: CustomEvent) => void;
  children?: preact.ComponentChildren;
  class?: string;
  id?: string;
  style?: string;
  title?: string;
  role?: string;
  slot?: string;
  part?: string;
  exportparts?: string;
  dir?: string;
  lang?: string;
  translate?: string;
  accesskey?: string;
  enterkeyhint?: string;
  inputmode?: string;
  popover?: string;
  contenteditable?: boolean | string;
  tabindex?: number;
  hidden?: boolean;
  inert?: boolean;
  draggable?: boolean;
  spellcheck?: boolean;
  autofocus?: boolean;
  [key: `data-${string}`]: unknown;
  [key: `aria-${string}`]: unknown;
  [key: `on${string}`]: unknown;
}

export const DataTable: FunctionComponent<DataTableProps> = ({ rows, sortable, selectable, sortColumn, sortDirection, virtual, rowHeight, onArcSort, onArcSelectAll, onArcRowSelect, children, ...rest }) => {
  const ref = useRef<HTMLElement>(null);
  useLayoutEffect(() => {
    const el = ref.current;
    if (!el) return;
    const listeners: Array<[string, EventListener]> = [];
    if (onArcSort) {
      const fn: EventListener = (e) => onArcSort(e as CustomEvent);
      el.addEventListener('arc-sort', fn);
      listeners.push(['arc-sort', fn]);
    }
    if (onArcSelectAll) {
      const fn: EventListener = (e) => onArcSelectAll(e as CustomEvent);
      el.addEventListener('arc-select-all', fn);
      listeners.push(['arc-select-all', fn]);
    }
    if (onArcRowSelect) {
      const fn: EventListener = (e) => onArcRowSelect(e as CustomEvent);
      el.addEventListener('arc-row-select', fn);
      listeners.push(['arc-row-select', fn]);
    }
    return () => listeners.forEach(([name, fn]) => el.removeEventListener(name, fn));
  }, [onArcSort, onArcSelectAll, onArcRowSelect]);
  return (
    <arc-data-table ref={ref} rows={rows} sortable={sortable} selectable={selectable} sortColumn={sortColumn} sortDirection={sortDirection} virtual={virtual} rowHeight={rowHeight} {...rest}>
      {children}
    </arc-data-table>
  );
};
