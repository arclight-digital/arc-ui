// Auto-generated by @arclux/prism 3.1.1 — do not edit manually

import { h } from 'preact';
import { forwardRef } from 'preact/compat';
import { useImperativeHandle, useLayoutEffect, useRef } from 'preact/hooks';
import '@arclux/arc-ui/terminal';
import type { ArcTerminal } from '@arclux/arc-ui/terminal';

export interface TerminalProps {
  lines?: unknown[];
  prompt?: string;
  title?: string;
  speed?: number;
  autoplay?: boolean;
  loop?: boolean;
  onArcComplete?: (e: CustomEvent) => void;
  class?: string;
  id?: string;
  style?: string;
  role?: string;
  slot?: string;
  part?: string;
  exportparts?: string;
  dir?: string;
  lang?: string;
  translate?: string;
  accesskey?: string;
  enterkeyhint?: string;
  inputmode?: string;
  popover?: string;
  contenteditable?: boolean | string;
  tabindex?: number;
  hidden?: boolean;
  inert?: boolean;
  draggable?: boolean;
  spellcheck?: boolean;
  autofocus?: boolean;
  [key: `data-${string}`]: unknown;
  [key: `aria-${string}`]: unknown;
  [key: `on${string}`]: unknown;
}

/** Driven by its methods: play() and reset(). A ref on it holds the element. */
export const Terminal = forwardRef<ArcTerminal, TerminalProps>(({ lines, prompt, title, speed, autoplay, loop, onArcComplete, ...rest }, forwarded) => {
  const ref = useRef<ArcTerminal>(null);
  useImperativeHandle(forwarded, () => ref.current as ArcTerminal, []);
  useLayoutEffect(() => {
    const el = ref.current;
    if (!el) return;
    const listeners: Array<[string, EventListener]> = [];
    if (onArcComplete) {
      const fn: EventListener = (e) => onArcComplete(e as CustomEvent);
      el.addEventListener('arc-complete', fn);
      listeners.push(['arc-complete', fn]);
    }
    return () => listeners.forEach(([name, fn]) => el.removeEventListener(name, fn));
  }, [onArcComplete]);
  return h('arc-terminal', { ref, lines, prompt, title, speed, autoplay, loop, ...rest });
});
