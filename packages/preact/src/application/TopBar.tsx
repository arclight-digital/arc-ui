// Auto-generated by @arclux/prism — do not edit manually

import { type FunctionComponent } from 'preact';
import '@arclux/arc-ui';

export interface TopBarProps {
  heading?: string;
  fixed?: boolean;
  menuOpen?: boolean;
  children?: preact.ComponentChildren;
  [key: string]: unknown;
}

export const TopBar: FunctionComponent<TopBarProps> = ({ heading, fixed, menuOpen, children, ...rest }) => (
  <arc-top-bar heading={heading} fixed={fixed} menuOpen={menuOpen} {...rest}>
    {children}
  </arc-top-bar>
);
