// Auto-generated by @arclux/prism 3.1.1 — do not edit manually

import { h } from 'preact';
import { forwardRef } from 'preact/compat';
import { useImperativeHandle, useLayoutEffect, useRef } from 'preact/hooks';
import '@arclux/arc-ui/form';
import type { ArcForm } from '@arclux/arc-ui/form';

export interface FormProps {
  action?: string;
  method?: string;
  novalidate?: boolean;
  loading?: boolean;
  disabled?: boolean;
  errorSummary?: boolean;
  onArcInvalid?: (e: CustomEvent) => void;
  onArcSubmit?: (e: CustomEvent) => void;
  onArcReset?: (e: CustomEvent) => void;
  children?: preact.ComponentChildren;
  class?: string;
  id?: string;
  style?: string;
  title?: string;
  role?: string;
  slot?: string;
  part?: string;
  exportparts?: string;
  dir?: string;
  lang?: string;
  translate?: string;
  accesskey?: string;
  enterkeyhint?: string;
  inputmode?: string;
  popover?: string;
  contenteditable?: boolean | string;
  tabindex?: number;
  hidden?: boolean;
  inert?: boolean;
  draggable?: boolean;
  spellcheck?: boolean;
  autofocus?: boolean;
  [key: `data-${string}`]: unknown;
  [key: `aria-${string}`]: unknown;
  [key: `on${string}`]: unknown;
}

/** Driven by its methods: submit() and reset(). A ref on it holds the element. */
export const Form = forwardRef<ArcForm, FormProps>(({ action, method, novalidate, loading, disabled, errorSummary, onArcInvalid, onArcSubmit, onArcReset, children, ...rest }, forwarded) => {
  const ref = useRef<ArcForm>(null);
  useImperativeHandle(forwarded, () => ref.current as ArcForm, []);
  useLayoutEffect(() => {
    const el = ref.current;
    if (!el) return;
    const listeners: Array<[string, EventListener]> = [];
    if (onArcInvalid) {
      const fn: EventListener = (e) => onArcInvalid(e as CustomEvent);
      el.addEventListener('arc-invalid', fn);
      listeners.push(['arc-invalid', fn]);
    }
    if (onArcSubmit) {
      const fn: EventListener = (e) => onArcSubmit(e as CustomEvent);
      el.addEventListener('arc-submit', fn);
      listeners.push(['arc-submit', fn]);
    }
    if (onArcReset) {
      const fn: EventListener = (e) => onArcReset(e as CustomEvent);
      el.addEventListener('arc-reset', fn);
      listeners.push(['arc-reset', fn]);
    }
    return () => listeners.forEach(([name, fn]) => el.removeEventListener(name, fn));
  }, [onArcInvalid, onArcSubmit, onArcReset]);
  return h('arc-form', { ref, action, method, novalidate, loading, disabled, errorSummary, ...rest }, children);
});
