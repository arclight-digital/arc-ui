// Auto-generated by @arclux/prism — do not edit manually

import { type FunctionComponent } from 'preact';
import { useLayoutEffect, useRef } from 'preact/hooks';
import '@arclux/arc-ui/copy-button';

export interface CopyButtonProps {
  value?: string;
  disabled?: boolean;
  onArcCopy?: (e: CustomEvent) => void;
  children?: preact.ComponentChildren;
  [key: string]: unknown;
}

export const CopyButton: FunctionComponent<CopyButtonProps> = ({ value, disabled, onArcCopy, children, ...rest }) => {
  const ref = useRef<HTMLElement>(null);
  useLayoutEffect(() => {
    const el = ref.current;
    if (!el) return;
    const listeners: Array<[string, EventListener]> = [];
    if (onArcCopy) {
      const fn: EventListener = (e) => onArcCopy(e as CustomEvent);
      el.addEventListener('arc-copy', fn);
      listeners.push(['arc-copy', fn]);
    }
    return () => listeners.forEach(([name, fn]) => el.removeEventListener(name, fn));
  }, [onArcCopy]);
  return (
    <arc-copy-button ref={ref} value={value} disabled={disabled} {...rest}>
      {children}
    </arc-copy-button>
  );
};
