// Auto-generated by @arclux/prism — do not edit manually

import { type FunctionComponent } from 'preact';
import { useLayoutEffect, useRef } from 'preact/hooks';
import '@arclux/arc-ui/hotkey';

export interface HotkeyProps {
  keys?: string;
  disabled?: boolean;
  global?: boolean;
  onArcHotkeyTrigger?: (e: CustomEvent) => void;
  children?: preact.ComponentChildren;
  [key: string]: unknown;
}

export const Hotkey: FunctionComponent<HotkeyProps> = ({ keys, disabled, global, onArcHotkeyTrigger, children, ...rest }) => {
  const ref = useRef<HTMLElement>(null);
  useLayoutEffect(() => {
    const el = ref.current;
    if (!el) return;
    const listeners: Array<[string, EventListener]> = [];
    if (onArcHotkeyTrigger) {
      const fn: EventListener = (e) => onArcHotkeyTrigger(e as CustomEvent);
      el.addEventListener('arc-hotkey-trigger', fn);
      listeners.push(['arc-hotkey-trigger', fn]);
    }
    return () => listeners.forEach(([name, fn]) => el.removeEventListener(name, fn));
  }, [onArcHotkeyTrigger]);
  return (
    <arc-hotkey ref={ref} keys={keys} disabled={disabled} global={global} {...rest}>
      {children}
    </arc-hotkey>
  );
};
