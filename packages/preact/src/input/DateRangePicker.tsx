// Auto-generated by @arclux/prism — do not edit manually

import { type FunctionComponent } from 'preact';
import '@arclux/arc-ui/date-range-picker';

export interface DateRangePickerProps {
  start?: string;
  end?: string;
  name?: string;
  min?: string;
  max?: string;
  months?: number;
  presets?: unknown[];
  placeholder?: string;
  disabled?: boolean;
  required?: boolean;
  label?: string;
  children?: preact.ComponentChildren;
  [key: string]: unknown;
}

export const DateRangePicker: FunctionComponent<DateRangePickerProps> = ({ start, end, name, min, max, months, presets, placeholder, disabled, required, label, children, ...rest }) => (
  <arc-date-range-picker start={start} end={end} name={name} min={min} max={max} months={months} presets={presets} placeholder={placeholder} disabled={disabled} required={required} label={label} {...rest}>
    {children}
  </arc-date-range-picker>
);
