// Auto-generated by @arclux/prism 3.1.1 — do not edit manually

import { h } from 'preact';
import { forwardRef } from 'preact/compat';
import { useImperativeHandle, useLayoutEffect, useRef } from 'preact/hooks';
import '@arclux/arc-ui/date-range-picker';
import type { ArcDateRangePicker } from '@arclux/arc-ui/date-range-picker';

export interface DateRangePickerProps {
  required?: boolean;
  readonly?: boolean;
  size?: 'sm' | 'md' | 'lg';
  locale?: string;
  firstDayOfWeek?: number;
  start?: string;
  end?: string;
  name?: string;
  min?: string;
  max?: string;
  months?: number;
  presets?: Array<{label:string,days:number}>;
  placeholder?: string;
  disabled?: boolean;
  label?: string;
  open?: boolean;
  onArcChange?: (e: CustomEvent) => void;
  class?: string;
  id?: string;
  style?: string;
  title?: string;
  role?: string;
  slot?: string;
  part?: string;
  exportparts?: string;
  dir?: string;
  lang?: string;
  translate?: string;
  accesskey?: string;
  enterkeyhint?: string;
  inputmode?: string;
  popover?: string;
  contenteditable?: boolean | string;
  tabindex?: number;
  hidden?: boolean;
  inert?: boolean;
  draggable?: boolean;
  spellcheck?: boolean;
  autofocus?: boolean;
  [key: `data-${string}`]: unknown;
  [key: `aria-${string}`]: unknown;
  [key: `on${string}`]: unknown;
}

/** Driven by its methods: checkValidity() and reportValidity(). A ref on it holds the element. */
export const DateRangePicker = forwardRef<ArcDateRangePicker, DateRangePickerProps>(({ required, readonly, size, locale, firstDayOfWeek, start, end, name, min, max, months, presets, placeholder, disabled, label, open, onArcChange, ...rest }, forwarded) => {
  const ref = useRef<ArcDateRangePicker>(null);
  useImperativeHandle(forwarded, () => ref.current as ArcDateRangePicker, []);
  useLayoutEffect(() => {
    const el = ref.current;
    if (!el) return;
    const listeners: Array<[string, EventListener]> = [];
    if (onArcChange) {
      const fn: EventListener = (e) => onArcChange(e as CustomEvent);
      el.addEventListener('arc-change', fn);
      listeners.push(['arc-change', fn]);
    }
    return () => listeners.forEach(([name, fn]) => el.removeEventListener(name, fn));
  }, [onArcChange]);
  return h('arc-date-range-picker', { ref, required, readonly, size, locale, firstDayOfWeek, start, end, name, min, max, months, presets, placeholder, disabled, label, open, ...rest });
});
