// Auto-generated by @arclux/prism 3.1.1 — do not edit manually

import { h } from 'preact';
import { forwardRef } from 'preact/compat';
import { useImperativeHandle, useLayoutEffect, useRef } from 'preact/hooks';
import '@arclux/arc-ui/password-input';
import type { ArcPasswordInput } from '@arclux/arc-ui/password-input';

export interface PasswordInputProps {
  required?: boolean;
  readonly?: boolean;
  name?: string;
  label?: string;
  placeholder?: string;
  value?: string;
  disabled?: boolean;
  error?: string;
  size?: 'sm' | 'md' | 'lg';
  autocomplete?: string;
  showStrength?: boolean;
  onArcStrengthChange?: (e: CustomEvent) => void;
  onArcInput?: (e: CustomEvent) => void;
  onArcChange?: (e: CustomEvent) => void;
  class?: string;
  id?: string;
  style?: string;
  title?: string;
  role?: string;
  slot?: string;
  part?: string;
  exportparts?: string;
  dir?: string;
  lang?: string;
  translate?: string;
  accesskey?: string;
  enterkeyhint?: string;
  inputmode?: string;
  popover?: string;
  contenteditable?: boolean | string;
  tabindex?: number;
  hidden?: boolean;
  inert?: boolean;
  draggable?: boolean;
  spellcheck?: boolean;
  autofocus?: boolean;
  [key: `data-${string}`]: unknown;
  [key: `aria-${string}`]: unknown;
  [key: `on${string}`]: unknown;
}

/** Driven by its methods: checkValidity() and reportValidity(). A ref on it holds the element. */
export const PasswordInput = forwardRef<ArcPasswordInput, PasswordInputProps>(({ required, readonly, name, label, placeholder, value, disabled, error, size, autocomplete, showStrength, onArcStrengthChange, onArcInput, onArcChange, ...rest }, forwarded) => {
  const ref = useRef<ArcPasswordInput>(null);
  useImperativeHandle(forwarded, () => ref.current as ArcPasswordInput, []);
  useLayoutEffect(() => {
    const el = ref.current;
    if (!el) return;
    const listeners: Array<[string, EventListener]> = [];
    if (onArcStrengthChange) {
      const fn: EventListener = (e) => onArcStrengthChange(e as CustomEvent);
      el.addEventListener('arc-strength-change', fn);
      listeners.push(['arc-strength-change', fn]);
    }
    if (onArcInput) {
      const fn: EventListener = (e) => onArcInput(e as CustomEvent);
      el.addEventListener('arc-input', fn);
      listeners.push(['arc-input', fn]);
    }
    if (onArcChange) {
      const fn: EventListener = (e) => onArcChange(e as CustomEvent);
      el.addEventListener('arc-change', fn);
      listeners.push(['arc-change', fn]);
    }
    return () => listeners.forEach(([name, fn]) => el.removeEventListener(name, fn));
  }, [onArcStrengthChange, onArcInput, onArcChange]);
  return h('arc-password-input', { ref, required, readonly, name, label, placeholder, value, disabled, error, size, autocomplete, showStrength, ...rest });
});
