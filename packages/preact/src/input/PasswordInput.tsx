// Auto-generated by @arclux/prism — do not edit manually

import { type FunctionComponent } from 'preact';
import '@arclux/arc-ui/password-input';

export interface PasswordInputProps {
  name?: string;
  label?: string;
  placeholder?: string;
  value?: string;
  disabled?: boolean;
  required?: boolean;
  error?: string;
  size?: 'sm' | 'lg';
  autocomplete?: string;
  showStrength?: boolean;
  children?: preact.ComponentChildren;
  [key: string]: unknown;
}

export const PasswordInput: FunctionComponent<PasswordInputProps> = ({ name, label, placeholder, value, disabled, required, error, size, autocomplete, showStrength, children, ...rest }) => (
  <arc-password-input name={name} label={label} placeholder={placeholder} value={value} disabled={disabled} required={required} error={error} size={size} autocomplete={autocomplete} showStrength={showStrength} {...rest}>
    {children}
  </arc-password-input>
);
