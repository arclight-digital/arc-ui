// Auto-generated by @arclux/prism — do not edit manually

import { type FunctionComponent } from 'preact';
import { useLayoutEffect, useRef } from 'preact/hooks';
import '@arclux/arc-ui/password-input';

export interface PasswordInputProps {
  name?: string;
  label?: string;
  placeholder?: string;
  value?: string;
  disabled?: boolean;
  required?: boolean;
  error?: string;
  size?: 'sm' | 'lg';
  autocomplete?: string;
  showStrength?: boolean;
  onArcStrengthChange?: (e: CustomEvent) => void;
  onArcInput?: (e: CustomEvent) => void;
  onArcChange?: (e: CustomEvent) => void;
  children?: preact.ComponentChildren;
  [key: string]: unknown;
}

export const PasswordInput: FunctionComponent<PasswordInputProps> = ({ name, label, placeholder, value, disabled, required, error, size, autocomplete, showStrength, onArcStrengthChange, onArcInput, onArcChange, children, ...rest }) => {
  const ref = useRef<HTMLElement>(null);
  useLayoutEffect(() => {
    const el = ref.current;
    if (!el) return;
    const listeners: Array<[string, EventListener]> = [];
    if (onArcStrengthChange) {
      const fn: EventListener = (e) => onArcStrengthChange(e as CustomEvent);
      el.addEventListener('arc-strength-change', fn);
      listeners.push(['arc-strength-change', fn]);
    }
    if (onArcInput) {
      const fn: EventListener = (e) => onArcInput(e as CustomEvent);
      el.addEventListener('arc-input', fn);
      listeners.push(['arc-input', fn]);
    }
    if (onArcChange) {
      const fn: EventListener = (e) => onArcChange(e as CustomEvent);
      el.addEventListener('arc-change', fn);
      listeners.push(['arc-change', fn]);
    }
    return () => listeners.forEach(([name, fn]) => el.removeEventListener(name, fn));
  }, [onArcStrengthChange, onArcInput, onArcChange]);
  return (
    <arc-password-input ref={ref} name={name} label={label} placeholder={placeholder} value={value} disabled={disabled} required={required} error={error} size={size} autocomplete={autocomplete} showStrength={showStrength} {...rest}>
      {children}
    </arc-password-input>
  );
};
