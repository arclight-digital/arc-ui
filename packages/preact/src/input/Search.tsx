// Auto-generated by @arclux/prism — do not edit manually

import { type FunctionComponent } from 'preact';
import { useLayoutEffect, useRef } from 'preact/hooks';
import '@arclux/arc-ui/search';

export interface SearchProps {
  value?: string;
  placeholder?: string;
  label?: string;
  disabled?: boolean;
  loading?: boolean;
  onArcInput?: (e: CustomEvent) => void;
  onArcClear?: (e: CustomEvent) => void;
  onArcChange?: (e: CustomEvent) => void;
  onArcSelect?: (e: CustomEvent) => void;
  children?: preact.ComponentChildren;
  [key: string]: unknown;
}

export const Search: FunctionComponent<SearchProps> = ({ value, placeholder, label, disabled, loading, onArcInput, onArcClear, onArcChange, onArcSelect, children, ...rest }) => {
  const ref = useRef<HTMLElement>(null);
  useLayoutEffect(() => {
    const el = ref.current;
    if (!el) return;
    const listeners: Array<[string, EventListener]> = [];
    if (onArcInput) {
      const fn: EventListener = (e) => onArcInput(e as CustomEvent);
      el.addEventListener('arc-input', fn);
      listeners.push(['arc-input', fn]);
    }
    if (onArcClear) {
      const fn: EventListener = (e) => onArcClear(e as CustomEvent);
      el.addEventListener('arc-clear', fn);
      listeners.push(['arc-clear', fn]);
    }
    if (onArcChange) {
      const fn: EventListener = (e) => onArcChange(e as CustomEvent);
      el.addEventListener('arc-change', fn);
      listeners.push(['arc-change', fn]);
    }
    if (onArcSelect) {
      const fn: EventListener = (e) => onArcSelect(e as CustomEvent);
      el.addEventListener('arc-select', fn);
      listeners.push(['arc-select', fn]);
    }
    return () => listeners.forEach(([name, fn]) => el.removeEventListener(name, fn));
  }, [onArcInput, onArcClear, onArcChange, onArcSelect]);
  return (
    <arc-search ref={ref} value={value} placeholder={placeholder} label={label} disabled={disabled} loading={loading} {...rest}>
      {children}
    </arc-search>
  );
};
