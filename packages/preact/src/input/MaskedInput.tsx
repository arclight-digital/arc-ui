// Auto-generated by @arclux/prism 3.1.1 — do not edit manually

import { h } from 'preact';
import { forwardRef } from 'preact/compat';
import { useImperativeHandle, useLayoutEffect, useRef } from 'preact/hooks';
import '@arclux/arc-ui/masked-input';
import type { ArcMaskedInput } from '@arclux/arc-ui/masked-input';

export interface MaskedInputProps {
  required?: boolean;
  readonly?: boolean;
  mask?: string;
  value?: string;
  placeholderChar?: string;
  label?: string;
  name?: string;
  disabled?: boolean;
  autocomplete?: string;
  error?: string;
  size?: 'sm' | 'md' | 'lg';
  onArcInput?: (e: CustomEvent) => void;
  onArcChange?: (e: CustomEvent) => void;
  children?: preact.ComponentChildren;
  class?: string;
  id?: string;
  style?: string;
  title?: string;
  role?: string;
  slot?: string;
  part?: string;
  exportparts?: string;
  dir?: string;
  lang?: string;
  translate?: string;
  accesskey?: string;
  enterkeyhint?: string;
  inputmode?: string;
  popover?: string;
  contenteditable?: boolean | string;
  tabindex?: number;
  hidden?: boolean;
  inert?: boolean;
  draggable?: boolean;
  spellcheck?: boolean;
  autofocus?: boolean;
  [key: `data-${string}`]: unknown;
  [key: `aria-${string}`]: unknown;
  [key: `on${string}`]: unknown;
}

/** Driven by its methods: checkValidity() and reportValidity(). A ref on it holds the element. */
export const MaskedInput = forwardRef<ArcMaskedInput, MaskedInputProps>(({ required, readonly, mask, value, placeholderChar, label, name, disabled, autocomplete, error, size, onArcInput, onArcChange, children, ...rest }, forwarded) => {
  const ref = useRef<ArcMaskedInput>(null);
  useImperativeHandle(forwarded, () => ref.current as ArcMaskedInput, []);
  useLayoutEffect(() => {
    const el = ref.current;
    if (!el) return;
    const listeners: Array<[string, EventListener]> = [];
    if (onArcInput) {
      const fn: EventListener = (e) => onArcInput(e as CustomEvent);
      el.addEventListener('arc-input', fn);
      listeners.push(['arc-input', fn]);
    }
    if (onArcChange) {
      const fn: EventListener = (e) => onArcChange(e as CustomEvent);
      el.addEventListener('arc-change', fn);
      listeners.push(['arc-change', fn]);
    }
    return () => listeners.forEach(([name, fn]) => el.removeEventListener(name, fn));
  }, [onArcInput, onArcChange]);
  return h('arc-masked-input', { ref, required, readonly, mask, value, placeholderChar, label, name, disabled, autocomplete, error, size, ...rest }, children);
});
