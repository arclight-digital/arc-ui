// Auto-generated by @arclux/prism — do not edit manually

import { type FunctionComponent } from 'preact';
import '@arclux/arc-ui/range-slider';

export interface RangeSliderProps {
  min?: number;
  max?: number;
  step?: number;
  low?: number;
  high?: number;
  name?: string;
  disabled?: boolean;
  label?: string;
  showValues?: boolean;
  children?: preact.ComponentChildren;
  [key: string]: unknown;
}

export const RangeSlider: FunctionComponent<RangeSliderProps> = ({ min, max, step, low, high, name, disabled, label, showValues, children, ...rest }) => (
  <arc-range-slider min={min} max={max} step={step} low={low} high={high} name={name} disabled={disabled} label={label} showValues={showValues} {...rest}>
    {children}
  </arc-range-slider>
);
