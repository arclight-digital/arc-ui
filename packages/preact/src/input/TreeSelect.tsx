// Auto-generated by @arclux/prism 3.1.1 — do not edit manually

import { h } from 'preact';
import { forwardRef } from 'preact/compat';
import { useImperativeHandle, useLayoutEffect, useRef } from 'preact/hooks';
import '@arclux/arc-ui/tree-select';
import type { ArcTreeSelect } from '@arclux/arc-ui/tree-select';

export interface TreeSelectProps {
  required?: boolean;
  readonly?: boolean;
  items?: Array<{value: string, label: string, children?: Array<object>, disabled?: boolean}>;
  value?: string;
  expandedValues?: string[];
  placeholder?: string;
  label?: string;
  name?: string;
  disabled?: boolean;
  size?: 'sm' | 'md' | 'lg';
  error?: string;
  open?: boolean;
  onArcChange?: (e: CustomEvent) => void;
  class?: string;
  id?: string;
  style?: string;
  title?: string;
  role?: string;
  slot?: string;
  part?: string;
  exportparts?: string;
  dir?: string;
  lang?: string;
  translate?: string;
  accesskey?: string;
  enterkeyhint?: string;
  inputmode?: string;
  popover?: string;
  contenteditable?: boolean | string;
  tabindex?: number;
  hidden?: boolean;
  inert?: boolean;
  draggable?: boolean;
  spellcheck?: boolean;
  autofocus?: boolean;
  [key: `data-${string}`]: unknown;
  [key: `aria-${string}`]: unknown;
  [key: `on${string}`]: unknown;
}

/** Driven by its methods: checkValidity() and reportValidity(). A ref on it holds the element. */
export const TreeSelect = forwardRef<ArcTreeSelect, TreeSelectProps>(({ required, readonly, items, value, expandedValues, placeholder, label, name, disabled, size, error, open, onArcChange, ...rest }, forwarded) => {
  const ref = useRef<ArcTreeSelect>(null);
  useImperativeHandle(forwarded, () => ref.current as ArcTreeSelect, []);
  useLayoutEffect(() => {
    const el = ref.current;
    if (!el) return;
    const listeners: Array<[string, EventListener]> = [];
    if (onArcChange) {
      const fn: EventListener = (e) => onArcChange(e as CustomEvent);
      el.addEventListener('arc-change', fn);
      listeners.push(['arc-change', fn]);
    }
    return () => listeners.forEach(([name, fn]) => el.removeEventListener(name, fn));
  }, [onArcChange]);
  return h('arc-tree-select', { ref, required, readonly, items, value, expandedValues, placeholder, label, name, disabled, size, error, open, ...rest });
});
