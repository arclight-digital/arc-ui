// Auto-generated by @arclux/prism 3.1.1 — do not edit manually

import { h } from 'preact';
import { forwardRef } from 'preact/compat';
import { useImperativeHandle, useLayoutEffect, useRef } from 'preact/hooks';
import '@arclux/arc-ui/signature-pad';
import type { ArcSignaturePad } from '@arclux/arc-ui/signature-pad';

export interface SignaturePadProps {
  required?: boolean;
  readonly?: boolean;
  value?: string;
  name?: string;
  label?: string;
  disabled?: boolean;
  penColor?: string;
  penWidth?: number;
  onArcClear?: (e: CustomEvent) => void;
  onArcInput?: (e: CustomEvent) => void;
  onArcChange?: (e: CustomEvent) => void;
  class?: string;
  id?: string;
  style?: string;
  title?: string;
  role?: string;
  slot?: string;
  part?: string;
  exportparts?: string;
  dir?: string;
  lang?: string;
  translate?: string;
  accesskey?: string;
  enterkeyhint?: string;
  inputmode?: string;
  popover?: string;
  contenteditable?: boolean | string;
  tabindex?: number;
  hidden?: boolean;
  inert?: boolean;
  draggable?: boolean;
  spellcheck?: boolean;
  autofocus?: boolean;
  [key: `data-${string}`]: unknown;
  [key: `aria-${string}`]: unknown;
  [key: `on${string}`]: unknown;
}

/** Driven by its methods: clear(), toDataURL(), checkValidity() and reportValidity(). A ref on it holds the element. */
export const SignaturePad = forwardRef<ArcSignaturePad, SignaturePadProps>(({ required, readonly, value, name, label, disabled, penColor, penWidth, onArcClear, onArcInput, onArcChange, ...rest }, forwarded) => {
  const ref = useRef<ArcSignaturePad>(null);
  useImperativeHandle(forwarded, () => ref.current as ArcSignaturePad, []);
  useLayoutEffect(() => {
    const el = ref.current;
    if (!el) return;
    const listeners: Array<[string, EventListener]> = [];
    if (onArcClear) {
      const fn: EventListener = (e) => onArcClear(e as CustomEvent);
      el.addEventListener('arc-clear', fn);
      listeners.push(['arc-clear', fn]);
    }
    if (onArcInput) {
      const fn: EventListener = (e) => onArcInput(e as CustomEvent);
      el.addEventListener('arc-input', fn);
      listeners.push(['arc-input', fn]);
    }
    if (onArcChange) {
      const fn: EventListener = (e) => onArcChange(e as CustomEvent);
      el.addEventListener('arc-change', fn);
      listeners.push(['arc-change', fn]);
    }
    return () => listeners.forEach(([name, fn]) => el.removeEventListener(name, fn));
  }, [onArcClear, onArcInput, onArcChange]);
  return h('arc-signature-pad', { ref, required, readonly, value, name, label, disabled, penColor, penWidth, ...rest });
});
