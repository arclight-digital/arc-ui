// Auto-generated by @arclux/prism 3.1.1 — do not edit manually

import { h } from 'preact';
import { forwardRef } from 'preact/compat';
import { useImperativeHandle, useLayoutEffect, useRef } from 'preact/hooks';
import '@arclux/arc-ui/image-cropper';
import type { ArcImageCropper } from '@arclux/arc-ui/image-cropper';

export interface ImageCropperProps {
  src?: string;
  height?: number;
  aspect?: number;
  zoom?: number;
  onArcCropChange?: (e: CustomEvent) => void;
  class?: string;
  id?: string;
  style?: string;
  title?: string;
  role?: string;
  slot?: string;
  part?: string;
  exportparts?: string;
  dir?: string;
  lang?: string;
  translate?: string;
  accesskey?: string;
  enterkeyhint?: string;
  inputmode?: string;
  popover?: string;
  contenteditable?: boolean | string;
  tabindex?: number;
  hidden?: boolean;
  inert?: boolean;
  draggable?: boolean;
  spellcheck?: boolean;
  autofocus?: boolean;
  [key: `data-${string}`]: unknown;
  [key: `aria-${string}`]: unknown;
  [key: `on${string}`]: unknown;
}

/** Driven by its methods: getCrop(), getCroppedBlob() and getCroppedDataUrl(). A ref on it holds the element. */
export const ImageCropper = forwardRef<ArcImageCropper, ImageCropperProps>(({ src, height, aspect, zoom, onArcCropChange, ...rest }, forwarded) => {
  const ref = useRef<ArcImageCropper>(null);
  useImperativeHandle(forwarded, () => ref.current as ArcImageCropper, []);
  useLayoutEffect(() => {
    const el = ref.current;
    if (!el) return;
    const listeners: Array<[string, EventListener]> = [];
    if (onArcCropChange) {
      const fn: EventListener = (e) => onArcCropChange(e as CustomEvent);
      el.addEventListener('arc-crop-change', fn);
      listeners.push(['arc-crop-change', fn]);
    }
    return () => listeners.forEach(([name, fn]) => el.removeEventListener(name, fn));
  }, [onArcCropChange]);
  return h('arc-image-cropper', { ref, src, height, aspect, zoom, ...rest });
});
