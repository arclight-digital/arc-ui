// Auto-generated by @arclux/prism — do not edit manually

import { type FunctionComponent } from 'preact';
import { useLayoutEffect, useRef } from 'preact/hooks';
import '@arclux/arc-ui/image-cropper';

export interface ImageCropperProps {
  src?: string;
  height?: number;
  aspect?: number;
  zoom?: number;
  onArcCropChange?: (e: CustomEvent) => void;
  children?: preact.ComponentChildren;
  [key: string]: unknown;
}

export const ImageCropper: FunctionComponent<ImageCropperProps> = ({ src, height, aspect, zoom, onArcCropChange, children, ...rest }) => {
  const ref = useRef<HTMLElement>(null);
  useLayoutEffect(() => {
    const el = ref.current;
    if (!el) return;
    const listeners: Array<[string, EventListener]> = [];
    if (onArcCropChange) {
      const fn: EventListener = (e) => onArcCropChange(e as CustomEvent);
      el.addEventListener('arc-crop-change', fn);
      listeners.push(['arc-crop-change', fn]);
    }
    return () => listeners.forEach(([name, fn]) => el.removeEventListener(name, fn));
  }, [onArcCropChange]);
  return (
    <arc-image-cropper ref={ref} src={src} height={height} aspect={aspect} zoom={zoom} {...rest}>
      {children}
    </arc-image-cropper>
  );
};
