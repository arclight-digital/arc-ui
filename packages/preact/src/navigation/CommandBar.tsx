// Auto-generated by @arclux/prism — do not edit manually

import { type FunctionComponent } from 'preact';
import { useLayoutEffect, useRef } from 'preact/hooks';
import '@arclux/arc-ui/command-bar';

export interface CommandBarProps {
  placeholder?: string;
  value?: string;
  icon?: string;
  onArcInput?: (e: CustomEvent) => void;
  onArcSubmit?: (e: CustomEvent) => void;
  children?: preact.ComponentChildren;
  [key: string]: unknown;
}

export const CommandBar: FunctionComponent<CommandBarProps> = ({ placeholder, value, icon, onArcInput, onArcSubmit, children, ...rest }) => {
  const ref = useRef<HTMLElement>(null);
  useLayoutEffect(() => {
    const el = ref.current;
    if (!el) return;
    const listeners: Array<[string, EventListener]> = [];
    if (onArcInput) {
      const fn: EventListener = (e) => onArcInput(e as CustomEvent);
      el.addEventListener('arc-input', fn);
      listeners.push(['arc-input', fn]);
    }
    if (onArcSubmit) {
      const fn: EventListener = (e) => onArcSubmit(e as CustomEvent);
      el.addEventListener('arc-submit', fn);
      listeners.push(['arc-submit', fn]);
    }
    return () => listeners.forEach(([name, fn]) => el.removeEventListener(name, fn));
  }, [onArcInput, onArcSubmit]);
  return (
    <arc-command-bar ref={ref} placeholder={placeholder} value={value} icon={icon} {...rest}>
      {children}
    </arc-command-bar>
  );
};
