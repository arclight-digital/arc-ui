// Auto-generated by @arclux/prism 3.1.1 — do not edit manually

import { h, type FunctionComponent } from 'preact';
import { useLayoutEffect, useRef } from 'preact/hooks';
import '@arclux/arc-ui/top-bar';

export interface TopBarProps {
  heading?: string;
  homeHref?: string;
  scrolled?: boolean;
  immersive?: boolean;
  fixed?: boolean;
  contained?: string;
  menuOpen?: boolean;
  mobileMenu?: string;
  menuPosition?: string;
  navAlign?: 'left' | 'center' | 'right';
  onArcSidebarToggle?: (e: CustomEvent) => void;
  onArcMobileMenuToggle?: (e: CustomEvent) => void;
  children?: preact.ComponentChildren;
  class?: string;
  id?: string;
  style?: string;
  title?: string;
  role?: string;
  slot?: string;
  part?: string;
  exportparts?: string;
  dir?: string;
  lang?: string;
  translate?: string;
  accesskey?: string;
  enterkeyhint?: string;
  inputmode?: string;
  popover?: string;
  contenteditable?: boolean | string;
  tabindex?: number;
  hidden?: boolean;
  inert?: boolean;
  draggable?: boolean;
  spellcheck?: boolean;
  autofocus?: boolean;
  [key: `data-${string}`]: unknown;
  [key: `aria-${string}`]: unknown;
  [key: `on${string}`]: unknown;
}

export const TopBar: FunctionComponent<TopBarProps> = ({ heading, homeHref, scrolled, immersive, fixed, contained, menuOpen, mobileMenu, menuPosition, navAlign, onArcSidebarToggle, onArcMobileMenuToggle, children, ...rest }) => {
  const ref = useRef<HTMLElement>(null);
  useLayoutEffect(() => {
    const el = ref.current;
    if (!el) return;
    const listeners: Array<[string, EventListener]> = [];
    if (onArcSidebarToggle) {
      const fn: EventListener = (e) => onArcSidebarToggle(e as CustomEvent);
      el.addEventListener('arc-sidebar-toggle', fn);
      listeners.push(['arc-sidebar-toggle', fn]);
    }
    if (onArcMobileMenuToggle) {
      const fn: EventListener = (e) => onArcMobileMenuToggle(e as CustomEvent);
      el.addEventListener('arc-mobile-menu-toggle', fn);
      listeners.push(['arc-mobile-menu-toggle', fn]);
    }
    return () => listeners.forEach(([name, fn]) => el.removeEventListener(name, fn));
  }, [onArcSidebarToggle, onArcMobileMenuToggle]);
  return h('arc-top-bar', { ref, heading, homeHref, scrolled, immersive, fixed, contained, menuOpen, mobileMenu, menuPosition, navAlign, ...rest }, children);
};
