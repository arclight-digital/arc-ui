// Auto-generated by @arclux/prism 3.1.1 — do not edit manually

import { h } from 'preact';
import { forwardRef } from 'preact/compat';
import { useImperativeHandle, useLayoutEffect, useRef } from 'preact/hooks';
import '@arclux/arc-ui/sidebar-section';
import type { ArcSidebarSection } from '@arclux/arc-ui/sidebar-section';

export interface SidebarSectionProps {
  heading?: string;
  collapsible?: boolean;
  open?: boolean;
  icon?: string;
  onArcToggle?: (e: CustomEvent) => void;
  children?: preact.ComponentChildren;
  class?: string;
  id?: string;
  style?: string;
  title?: string;
  role?: string;
  slot?: string;
  part?: string;
  exportparts?: string;
  dir?: string;
  lang?: string;
  translate?: string;
  accesskey?: string;
  enterkeyhint?: string;
  inputmode?: string;
  popover?: string;
  contenteditable?: boolean | string;
  tabindex?: number;
  hidden?: boolean;
  inert?: boolean;
  draggable?: boolean;
  spellcheck?: boolean;
  autofocus?: boolean;
  [key: `data-${string}`]: unknown;
  [key: `aria-${string}`]: unknown;
  [key: `on${string}`]: unknown;
}

/** Driven by its methods: toggle(). A ref on it holds the element. */
export const SidebarSection = forwardRef<ArcSidebarSection, SidebarSectionProps>(({ heading, collapsible, open, icon, onArcToggle, children, ...rest }, forwarded) => {
  const ref = useRef<ArcSidebarSection>(null);
  useImperativeHandle(forwarded, () => ref.current as ArcSidebarSection, []);
  useLayoutEffect(() => {
    const el = ref.current;
    if (!el) return;
    const listeners: Array<[string, EventListener]> = [];
    if (onArcToggle) {
      const fn: EventListener = (e) => onArcToggle(e as CustomEvent);
      el.addEventListener('arc-toggle', fn);
      listeners.push(['arc-toggle', fn]);
    }
    return () => listeners.forEach(([name, fn]) => el.removeEventListener(name, fn));
  }, [onArcToggle]);
  return h('arc-sidebar-section', { ref, heading, collapsible, open, icon, ...rest }, children);
});
