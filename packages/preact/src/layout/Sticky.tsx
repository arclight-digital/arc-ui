// Auto-generated by @arclux/prism — do not edit manually

import { type FunctionComponent } from 'preact';
import { useLayoutEffect, useRef } from 'preact/hooks';
import '@arclux/arc-ui/sticky';

export interface StickyProps {
  offset?: string;
  onArcStuck?: (e: CustomEvent) => void;
  children?: preact.ComponentChildren;
  [key: string]: unknown;
}

export const Sticky: FunctionComponent<StickyProps> = ({ offset, onArcStuck, children, ...rest }) => {
  const ref = useRef<HTMLElement>(null);
  useLayoutEffect(() => {
    const el = ref.current;
    if (!el) return;
    const listeners: Array<[string, EventListener]> = [];
    if (onArcStuck) {
      const fn: EventListener = (e) => onArcStuck(e as CustomEvent);
      el.addEventListener('arc-stuck', fn);
      listeners.push(['arc-stuck', fn]);
    }
    return () => listeners.forEach(([name, fn]) => el.removeEventListener(name, fn));
  }, [onArcStuck]);
  return (
    <arc-sticky ref={ref} offset={offset} {...rest}>
      {children}
    </arc-sticky>
  );
};
