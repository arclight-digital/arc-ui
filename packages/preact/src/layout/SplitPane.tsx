// Auto-generated by @arclux/prism — do not edit manually

import { type FunctionComponent } from 'preact';
import { useLayoutEffect, useRef } from 'preact/hooks';
import '@arclux/arc-ui/split-pane';

export interface SplitPaneProps {
  orientation?: 'vertical' | 'horizontal';
  ratio?: number;
  minRatio?: number;
  maxRatio?: number;
  onArcResize?: (e: CustomEvent) => void;
  children?: preact.ComponentChildren;
  [key: string]: unknown;
}

export const SplitPane: FunctionComponent<SplitPaneProps> = ({ orientation, ratio, minRatio, maxRatio, onArcResize, children, ...rest }) => {
  const ref = useRef<HTMLElement>(null);
  useLayoutEffect(() => {
    const el = ref.current;
    if (!el) return;
    const listeners: Array<[string, EventListener]> = [];
    if (onArcResize) {
      const fn: EventListener = (e) => onArcResize(e as CustomEvent);
      el.addEventListener('arc-resize', fn);
      listeners.push(['arc-resize', fn]);
    }
    return () => listeners.forEach(([name, fn]) => el.removeEventListener(name, fn));
  }, [onArcResize]);
  return (
    <arc-split-pane ref={ref} orientation={orientation} ratio={ratio} minRatio={minRatio} maxRatio={maxRatio} {...rest}>
      {children}
    </arc-split-pane>
  );
};
