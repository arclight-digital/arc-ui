// Auto-generated by @arclux/prism 3.1.1 — do not edit manually

import { h, type FunctionComponent } from 'preact';
import { useLayoutEffect, useRef } from 'preact/hooks';
import '@arclux/arc-ui/app-shell';

export interface AppShellProps {
  sidebarOpen?: boolean;
  embedded?: boolean;
  breakpoint?: number;
  onArcSidebarToggle?: (e: CustomEvent) => void;
  children?: preact.ComponentChildren;
  class?: string;
  id?: string;
  style?: string;
  title?: string;
  role?: string;
  slot?: string;
  part?: string;
  exportparts?: string;
  dir?: string;
  lang?: string;
  translate?: string;
  accesskey?: string;
  enterkeyhint?: string;
  inputmode?: string;
  popover?: string;
  contenteditable?: boolean | string;
  tabindex?: number;
  hidden?: boolean;
  inert?: boolean;
  draggable?: boolean;
  spellcheck?: boolean;
  autofocus?: boolean;
  [key: `data-${string}`]: unknown;
  [key: `aria-${string}`]: unknown;
  [key: `on${string}`]: unknown;
}

export const AppShell: FunctionComponent<AppShellProps> = ({ sidebarOpen, embedded, breakpoint, onArcSidebarToggle, children, ...rest }) => {
  const ref = useRef<HTMLElement>(null);
  useLayoutEffect(() => {
    const el = ref.current;
    if (!el) return;
    const listeners: Array<[string, EventListener]> = [];
    if (onArcSidebarToggle) {
      const fn: EventListener = (e) => onArcSidebarToggle(e as CustomEvent);
      el.addEventListener('arc-sidebar-toggle', fn);
      listeners.push(['arc-sidebar-toggle', fn]);
    }
    return () => listeners.forEach(([name, fn]) => el.removeEventListener(name, fn));
  }, [onArcSidebarToggle]);
  return h('arc-app-shell', { ref, sidebarOpen, embedded, breakpoint, ...rest }, children);
};
