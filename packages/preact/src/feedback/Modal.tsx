// Auto-generated by @arclux/prism — do not edit manually

import { type FunctionComponent } from 'preact';
import { useLayoutEffect, useRef } from 'preact/hooks';
import '@arclux/arc-ui/modal';

export interface ModalProps {
  open?: boolean;
  heading?: string;
  size?: 'sm' | 'md' | 'lg';
  fullscreen?: boolean;
  closable?: boolean;
  onArcClose?: (e: CustomEvent) => void;
  onArcOpen?: (e: CustomEvent) => void;
  children?: preact.ComponentChildren;
  [key: string]: unknown;
}

export const Modal: FunctionComponent<ModalProps> = ({ open, heading, size, fullscreen, closable, onArcClose, onArcOpen, children, ...rest }) => {
  const ref = useRef<HTMLElement>(null);
  useLayoutEffect(() => {
    const el = ref.current;
    if (!el) return;
    const listeners: Array<[string, EventListener]> = [];
    if (onArcClose) {
      const fn: EventListener = (e) => onArcClose(e as CustomEvent);
      el.addEventListener('arc-close', fn);
      listeners.push(['arc-close', fn]);
    }
    if (onArcOpen) {
      const fn: EventListener = (e) => onArcOpen(e as CustomEvent);
      el.addEventListener('arc-open', fn);
      listeners.push(['arc-open', fn]);
    }
    return () => listeners.forEach(([name, fn]) => el.removeEventListener(name, fn));
  }, [onArcClose, onArcOpen]);
  return (
    <arc-modal ref={ref} open={open} heading={heading} size={size} fullscreen={fullscreen} closable={closable} {...rest}>
      {children}
    </arc-modal>
  );
};
