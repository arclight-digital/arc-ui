// Auto-generated by @arclux/prism — do not edit manually

import { type FunctionComponent } from 'preact';
import { useLayoutEffect, useRef } from 'preact/hooks';
import '@arclux/arc-ui/confirm';

export interface ConfirmProps {
  open?: boolean;
  heading?: string;
  message?: string;
  variant?: string;
  onArcConfirm?: (e: CustomEvent) => void;
  onArcCancel?: (e: CustomEvent) => void;
  children?: preact.ComponentChildren;
  [key: string]: unknown;
}

export const Confirm: FunctionComponent<ConfirmProps> = ({ open, heading, message, variant, onArcConfirm, onArcCancel, children, ...rest }) => {
  const ref = useRef<HTMLElement>(null);
  useLayoutEffect(() => {
    const el = ref.current;
    if (!el) return;
    const listeners: Array<[string, EventListener]> = [];
    if (onArcConfirm) {
      const fn: EventListener = (e) => onArcConfirm(e as CustomEvent);
      el.addEventListener('arc-confirm', fn);
      listeners.push(['arc-confirm', fn]);
    }
    if (onArcCancel) {
      const fn: EventListener = (e) => onArcCancel(e as CustomEvent);
      el.addEventListener('arc-cancel', fn);
      listeners.push(['arc-cancel', fn]);
    }
    return () => listeners.forEach(([name, fn]) => el.removeEventListener(name, fn));
  }, [onArcConfirm, onArcCancel]);
  return (
    <arc-confirm ref={ref} open={open} heading={heading} message={message} variant={variant} {...rest}>
      {children}
    </arc-confirm>
  );
};
