// Auto-generated by @arclux/prism 3.1.1 — do not edit manually

import { h, type FunctionComponent } from 'preact';
import { useLayoutEffect, useRef } from 'preact/hooks';
import '@arclux/arc-ui/connection-status';

export interface ConnectionStatusProps {
  onArcOnline?: (e: CustomEvent) => void;
  onArcOffline?: (e: CustomEvent) => void;
  class?: string;
  id?: string;
  style?: string;
  title?: string;
  role?: string;
  slot?: string;
  part?: string;
  exportparts?: string;
  dir?: string;
  lang?: string;
  translate?: string;
  accesskey?: string;
  enterkeyhint?: string;
  inputmode?: string;
  popover?: string;
  contenteditable?: boolean | string;
  tabindex?: number;
  hidden?: boolean;
  inert?: boolean;
  draggable?: boolean;
  spellcheck?: boolean;
  autofocus?: boolean;
  [key: `data-${string}`]: unknown;
  [key: `aria-${string}`]: unknown;
  [key: `on${string}`]: unknown;
}

export const ConnectionStatus: FunctionComponent<ConnectionStatusProps> = ({ onArcOnline, onArcOffline, ...rest }) => {
  const ref = useRef<HTMLElement>(null);
  useLayoutEffect(() => {
    const el = ref.current;
    if (!el) return;
    const listeners: Array<[string, EventListener]> = [];
    if (onArcOnline) {
      const fn: EventListener = (e) => onArcOnline(e as CustomEvent);
      el.addEventListener('arc-online', fn);
      listeners.push(['arc-online', fn]);
    }
    if (onArcOffline) {
      const fn: EventListener = (e) => onArcOffline(e as CustomEvent);
      el.addEventListener('arc-offline', fn);
      listeners.push(['arc-offline', fn]);
    }
    return () => listeners.forEach(([name, fn]) => el.removeEventListener(name, fn));
  }, [onArcOnline, onArcOffline]);
  return h('arc-connection-status', { ref, ...rest });
};
