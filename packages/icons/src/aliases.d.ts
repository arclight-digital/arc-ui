/**
 * Types for aliases.js — see that file for what the table is and why it lives here.
 *
 * Hand-written rather than generated, because the table it describes is: the
 * shape is one map per library of canonical name → what that library calls it,
 * which is what `iconRegistry.register()` takes as its `aliases` option.
 */
export declare const aliases: Record<'phosphor' | 'lucide', Record<string, string>>;
