// Auto-generated by @arclux/prism — do not edit manually

import { Component, CUSTOM_ELEMENTS_SCHEMA, Input, Output, EventEmitter } from '@angular/core';
import '@arclux/arc-ui/toast-manager';

@Component({
  selector: 'arc-toast-manager',
  standalone: true,
  schemas: [CUSTOM_ELEMENTS_SCHEMA],
  template: `<arc-toast-manager [attr.position]="position" [attr.duration]="duration" [attr.maxVisible]="maxVisible" [dedupe]="dedupe" [attr.queueLimit]="queueLimit" (arc-queue-overflow)="arcQueueOverflow.emit($event)" (arc-dismiss)="arcDismiss.emit($event)" (arc-queue-change)="arcQueueChange.emit($event)"><ng-content /></arc-toast-manager>`,
})
export class ToastManager {
  @Input() position: string = 'top-right';
  @Input() duration: number = 4000;
  @Input() maxVisible: number = 3;
  @Input() dedupe: boolean = true;
  @Input() queueLimit: number = 20;
  @Output() arcQueueOverflow = new EventEmitter<CustomEvent>();
  @Output() arcDismiss = new EventEmitter<CustomEvent>();
  @Output() arcQueueChange = new EventEmitter<CustomEvent>();
}
