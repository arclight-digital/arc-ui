// Auto-generated by @arclux/prism — do not edit manually

import { Component, ElementRef, inject, Input, Output, EventEmitter } from '@angular/core';
import { ArcEventCalendar } from '@arclux/arc-ui/event-calendar';

@Component({
  selector: 'arc-event-calendar',
  standalone: true,
  template: `<ng-content />`,
  host: {
    '(arc-period-change)': '_onArcPeriodChange($event)',
    '(arc-date-click)': '_onArcDateClick($event)',
  },
})
export class EventCalendar {
  private readonly _el: ArcEventCalendar = inject(ElementRef).nativeElement;

  @Input() set locale(value: string) {
    this._el.locale = value;
  }
  get locale(): string {
    return this._el.locale;
  }

  @Input() set firstDayOfWeek(value: number) {
    this._el.firstDayOfWeek = value;
  }
  get firstDayOfWeek(): number {
    return this._el.firstDayOfWeek;
  }

  @Input() set events(value: Array<{date:string,end?:string,label:string,color?:number}>) {
    this._el.events = value;
  }
  get events(): Array<{date:string,end?:string,label:string,color?:number}> {
    return this._el.events;
  }

  @Input() set view(value: 'month' | 'week') {
    this._el.view = value;
  }
  get view(): 'month' | 'week' {
    return this._el.view;
  }

  @Input() set date(value: string) {
    this._el.date = value;
  }
  get date(): string {
    return this._el.date;
  }

  @Output() viewChange = new EventEmitter<'month' | 'week'>();

  @Output() dateChange = new EventEmitter<string>();

  _onArcPeriodChange(event: CustomEvent) {
    const detail = event.detail as Record<string, unknown> | null;
    if (!detail) return;
    if ('date' in detail) {
      this.dateChange.emit(detail.date as string);
    }
    if ('view' in detail) {
      this.viewChange.emit(detail.view as 'month' | 'week');
    }
  }

  _onArcDateClick(event: CustomEvent) {
    const detail = event.detail as Record<string, unknown> | null;
    if (!detail) return;
    if ('date' in detail) {
      this.dateChange.emit(detail.date as string);
    }
  }
}
