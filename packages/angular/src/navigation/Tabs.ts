// Auto-generated by @arclux/prism 3.1.1 — do not edit manually

import { Component, ElementRef, inject, Input, Output, EventEmitter } from '@angular/core';
import '@arclux/arc-ui/tabs';
import type { ArcTabs } from '@arclux/arc-ui/tabs';

@Component({
  selector: 'arc-tabs',
  standalone: true,
  template: `<ng-content />`,
  host: {
    '(arc-change)': '_onArcChange($event)',
  },
})
export class Tabs {
  private readonly _el: ArcTabs = inject(ElementRef).nativeElement;

  @Input() set selected(value: number) {
    this._el.selected = value;
  }
  get selected(): number {
    return this._el.selected;
  }

  @Input() set align(value: 'start' | 'center' | 'end') {
    this._el.align = value;
  }
  get align(): 'start' | 'center' | 'end' {
    return this._el.align;
  }

  @Input() set variant(value: 'underline' | 'pills') {
    this._el.variant = value;
  }
  get variant(): 'underline' | 'pills' {
    return this._el.variant;
  }

  @Input() set orientation(value: 'horizontal' | 'vertical') {
    this._el.orientation = value;
  }
  get orientation(): 'horizontal' | 'vertical' {
    return this._el.orientation;
  }

  @Output() selectedChange = new EventEmitter<number>();

  _onArcChange(event: CustomEvent) {
    const detail = event.detail as Record<string, unknown> | null;
    if (!detail) return;
    if ('value' in detail) {
      this.selectedChange.emit(detail.value as number);
    }
  }
}
