// Auto-generated by @arclux/prism — do not edit manually

import { Component, ElementRef, inject, Input, Output, EventEmitter } from '@angular/core';
import { ArcPageIndicator } from '@arclux/arc-ui/page-indicator';

@Component({
  selector: 'arc-page-indicator',
  standalone: true,
  template: ``,
  host: {
    '(arc-change)': '_onArcChange($event)',
  },
})
export class PageIndicator {
  private readonly _el: ArcPageIndicator = inject(ElementRef).nativeElement;

  @Input() set count(value: number) {
    this._el.count = value;
  }
  get count(): number {
    return this._el.count;
  }

  @Input() set value(value: number) {
    this._el.value = value;
  }
  get value(): number {
    return this._el.value;
  }

  @Input() set clickable(value: boolean) {
    this._el.clickable = value;
  }
  get clickable(): boolean {
    return this._el.clickable;
  }

  @Output() valueChange = new EventEmitter<number>();

  _onArcChange(event: CustomEvent) {
    const detail = event.detail as Record<string, unknown> | null;
    if (!detail) return;
    if ('value' in detail) {
      this.valueChange.emit(detail.value as number);
    }
  }
}
