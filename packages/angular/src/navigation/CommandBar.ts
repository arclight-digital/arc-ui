// Auto-generated by @arclux/prism — do not edit manually

import { Component, ElementRef, inject, Input, Output, EventEmitter } from '@angular/core';
import '@arclux/arc-ui/command-bar';
import type { ArcCommandBar } from '@arclux/arc-ui/command-bar';

@Component({
  selector: 'arc-command-bar',
  standalone: true,
  template: `<ng-content />`,
  host: {
    '(arc-input)': '_onArcInput($event)',
    '(arc-submit)': '_onArcSubmit($event)',
  },
})
export class CommandBar {
  private readonly _el: ArcCommandBar = inject(ElementRef).nativeElement;

  @Input() set placeholder(value: string) {
    this._el.placeholder = value;
  }
  get placeholder(): string {
    return this._el.placeholder;
  }

  @Input() set value(value: string) {
    this._el.value = value;
  }
  get value(): string {
    return this._el.value;
  }

  @Input() set icon(value: string) {
    this._el.icon = value;
  }
  get icon(): string {
    return this._el.icon;
  }

  @Input() set label(value: string) {
    this._el.label = value;
  }
  get label(): string {
    return this._el.label;
  }

  @Output() valueChange = new EventEmitter<string>();

  _onArcInput(event: CustomEvent) {
    const detail = event.detail as Record<string, unknown> | null;
    if (!detail) return;
    if ('value' in detail) {
      this.valueChange.emit(detail.value as string);
    }
  }

  _onArcSubmit(event: CustomEvent) {
    const detail = event.detail as Record<string, unknown> | null;
    if (!detail) return;
    if ('value' in detail) {
      this.valueChange.emit(detail.value as string);
    }
  }
}
