// Auto-generated by @arclux/prism — do not edit manually

import { Component, CUSTOM_ELEMENTS_SCHEMA, Input, Output, EventEmitter } from '@angular/core';
import '@arclux/arc-ui/command-bar';

@Component({
  selector: 'arc-command-bar',
  standalone: true,
  schemas: [CUSTOM_ELEMENTS_SCHEMA],
  template: `<arc-command-bar [attr.placeholder]="this.placeholder" [attr.value]="this.value" [attr.icon]="this.icon" (arc-input)="this.onArcInput($event)" (arc-submit)="this.onArcSubmit($event)"></arc-command-bar>`,
})
export class CommandBar {
  @Input() placeholder: string = 'Search…';
  @Input() value: string = '';
  @Input() icon: string = 'magnifying-glass';
  @Output() arcInput = new EventEmitter<CustomEvent>();
  @Output() arcSubmit = new EventEmitter<CustomEvent>();
  @Output() valueChange = new EventEmitter<string>();

  onArcInput(event: CustomEvent) {
    this.arcInput.emit(event);
    const detail = event.detail as Record<string, unknown> | null;
    if (!detail) return;
    if ('value' in detail) {
      const next = detail.value as string;
      this.value = next;
      this.valueChange.emit(next);
    }
  }

  onArcSubmit(event: CustomEvent) {
    this.arcSubmit.emit(event);
    const detail = event.detail as Record<string, unknown> | null;
    if (!detail) return;
    if ('value' in detail) {
      const next = detail.value as string;
      this.value = next;
      this.valueChange.emit(next);
    }
  }
}
