// Auto-generated by @arclux/prism 3.1.1 — do not edit manually

import { Component, ElementRef, inject, Input, Output, EventEmitter } from '@angular/core';
import '@arclux/arc-ui/image-compare';
import type { ArcImageCompare } from '@arclux/arc-ui/image-compare';

@Component({
  selector: 'arc-image-compare',
  standalone: true,
  template: `<ng-content />`,
  host: {
    '(arc-input)': '_onArcInput($event)',
    '(arc-change)': '_onArcChange($event)',
  },
})
export class ImageCompare {
  private readonly _el: ArcImageCompare = inject(ElementRef).nativeElement;

  @Input() set position(value: number) {
    this._el.position = value;
  }
  get position(): number {
    return this._el.position;
  }

  @Input() set orientation(value: 'horizontal' | 'vertical') {
    this._el.orientation = value;
  }
  get orientation(): 'horizontal' | 'vertical' {
    return this._el.orientation;
  }

  @Input() set beforeLabel(value: string) {
    this._el.beforeLabel = value;
  }
  get beforeLabel(): string {
    return this._el.beforeLabel;
  }

  @Input() set afterLabel(value: string) {
    this._el.afterLabel = value;
  }
  get afterLabel(): string {
    return this._el.afterLabel;
  }

  @Input() set label(value: string) {
    this._el.label = value;
  }
  get label(): string {
    return this._el.label;
  }

  @Output() positionChange = new EventEmitter<number>();

  _onArcInput(event: CustomEvent) {
    const detail = event.detail as Record<string, unknown> | null;
    if (!detail) return;
    if ('value' in detail) {
      this.positionChange.emit(detail.value as number);
    }
  }

  _onArcChange(event: CustomEvent) {
    const detail = event.detail as Record<string, unknown> | null;
    if (!detail) return;
    if ('value' in detail) {
      this.positionChange.emit(detail.value as number);
    }
  }
}
