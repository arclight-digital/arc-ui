// Auto-generated by @arclux/prism 3.1.1 — do not edit manually

import { Component, ElementRef, inject, Input, Output, EventEmitter } from '@angular/core';
import '@arclux/arc-ui/collapsible';
import type { ArcCollapsible } from '@arclux/arc-ui/collapsible';

@Component({
  selector: 'arc-collapsible',
  standalone: true,
  template: `<ng-content />`,
  host: {
    '(arc-toggle)': '_onArcToggle($event)',
  },
})
export class Collapsible {
  private readonly _el: ArcCollapsible = inject(ElementRef).nativeElement;

  @Input() set open(value: boolean) {
    this._el.open = value;
  }
  get open(): boolean {
    return this._el.open;
  }

  @Input() set heading(value: string) {
    this._el.heading = value;
  }
  get heading(): string {
    return this._el.heading;
  }

  @Output() openChange = new EventEmitter<boolean>();

  _onArcToggle(event: CustomEvent) {
    const detail = event.detail as Record<string, unknown> | null;
    if (!detail) return;
    if ('open' in detail) {
      this.openChange.emit(detail.open as boolean);
    }
  }
}
