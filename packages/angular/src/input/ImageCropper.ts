// Auto-generated by @arclux/prism — do not edit manually

import { Component, CUSTOM_ELEMENTS_SCHEMA, Input, Output, EventEmitter } from '@angular/core';
import '@arclux/arc-ui/image-cropper';

@Component({
  selector: 'arc-image-cropper',
  standalone: true,
  schemas: [CUSTOM_ELEMENTS_SCHEMA],
  template: `<arc-image-cropper [attr.src]="this.src" [attr.height]="this.height" [attr.aspect]="this.aspect" [attr.zoom]="this.zoom" (arc-crop-change)="this.arcCropChange.emit($event)"></arc-image-cropper>`,
})
export class ImageCropper {
  @Input() src: string = '';
  @Input() height: number = 320;
  @Input() aspect: number = 0;
  @Input() zoom: number = 1;
  @Output() arcCropChange = new EventEmitter<CustomEvent>();
}
