// Auto-generated by @arclux/prism 3.0.0 — do not edit manually

import { Component, ElementRef, inject, Input, Output, EventEmitter, forwardRef } from '@angular/core';
import { NG_VALUE_ACCESSOR, type ControlValueAccessor } from '@angular/forms';
import '@arclux/arc-ui/date-range-picker';
import type { ArcDateRangePicker } from '@arclux/arc-ui/date-range-picker';

@Component({
  selector: 'arc-date-range-picker',
  standalone: true,
  template: ``,
  host: {
    '(arc-change)': '_onArcChange($event)',
  },
  providers: [
    { provide: NG_VALUE_ACCESSOR, useExisting: forwardRef(() => DateRangePicker), multi: true },
  ],
})
export class DateRangePicker implements ControlValueAccessor {
  private readonly _el: ArcDateRangePicker = inject(ElementRef).nativeElement;

  @Input() set required(value: boolean) {
    this._el.required = value;
  }
  get required(): boolean {
    return this._el.required;
  }

  @Input() set readonly(value: boolean) {
    this._el.readonly = value;
  }
  get readonly(): boolean {
    return this._el.readonly;
  }

  @Input() set size(value: 'sm' | 'md' | 'lg') {
    this._el.size = value;
  }
  get size(): 'sm' | 'md' | 'lg' {
    return this._el.size;
  }

  @Input() set locale(value: string) {
    this._el.locale = value;
  }
  get locale(): string {
    return this._el.locale;
  }

  @Input() set firstDayOfWeek(value: number) {
    this._el.firstDayOfWeek = value;
  }
  get firstDayOfWeek(): number {
    return this._el.firstDayOfWeek;
  }

  @Input() set start(value: string) {
    this._el.start = value;
  }
  get start(): string {
    return this._el.start;
  }

  @Input() set end(value: string) {
    this._el.end = value;
  }
  get end(): string {
    return this._el.end;
  }

  @Input() set name(value: string) {
    this._el.name = value;
  }
  get name(): string {
    return this._el.name;
  }

  @Input() set min(value: string) {
    this._el.min = value;
  }
  get min(): string {
    return this._el.min;
  }

  @Input() set max(value: string) {
    this._el.max = value;
  }
  get max(): string {
    return this._el.max;
  }

  @Input() set months(value: number) {
    this._el.months = value;
  }
  get months(): number {
    return this._el.months;
  }

  @Input() set presets(value: Array<{label:string,days:number}>) {
    this._el.presets = value;
  }
  get presets(): Array<{label:string,days:number}> {
    return this._el.presets;
  }

  @Input() set placeholder(value: string) {
    this._el.placeholder = value;
  }
  get placeholder(): string {
    return this._el.placeholder;
  }

  @Input() set disabled(value: boolean) {
    this._el.disabled = value;
  }
  get disabled(): boolean {
    return this._el.disabled;
  }

  @Input() set label(value: string) {
    this._el.label = value;
  }
  get label(): string {
    return this._el.label;
  }

  @Input() set open(value: boolean) {
    this._el.open = value;
  }
  get open(): boolean {
    return this._el.open;
  }

  @Output() startChange = new EventEmitter<string>();

  @Output() endChange = new EventEmitter<string>();

  _onArcChange(event: CustomEvent) {
    const detail = event.detail as Record<string, unknown> | null;
    if (!detail) return;
    if ('end' in detail) {
      this.endChange.emit(detail.end as string);
    }
    if ('start' in detail) {
      this.startChange.emit(detail.start as string);
    }
  }

  /* ── ControlValueAccessor ──
     Bound to `start` / `end`, committed on `arc-change`.
     The listener lives in the constructor because the host metadata already
     maps that event to this component’s own @Output. writeValue deliberately
     does not call _onChangeFn: it is the form writing into the element, and
     echoing it back would mark the control dirty on every setValue. */
  private _onChangeFn: (value: { start: string; end: string }) => void = () => {};
  private _onTouchedFn: () => void = () => {};

  constructor() {
    this._el.addEventListener('arc-change', () => {
      this._onChangeFn({ start: this._el.start, end: this._el.end });
      this._onTouchedFn();
    });
  }

  writeValue(value: { start: string; end: string } | null | undefined): void {
    const next = value ?? { start: '', end: '' };
    this._el.start = next.start;
    this._el.end = next.end;
  }

  registerOnChange(fn: (value: { start: string; end: string }) => void): void {
    this._onChangeFn = fn;
  }

  registerOnTouched(fn: () => void): void {
    this._onTouchedFn = fn;
  }

  setDisabledState(isDisabled: boolean): void {
    this._el.disabled = isDisabled;
  }
}
