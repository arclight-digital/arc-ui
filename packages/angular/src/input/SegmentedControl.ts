// Auto-generated by @arclux/prism — do not edit manually

import { Component, CUSTOM_ELEMENTS_SCHEMA, Input, Output, EventEmitter } from '@angular/core';
import '@arclux/arc-ui/segmented-control';

@Component({
  selector: 'arc-segmented-control',
  standalone: true,
  schemas: [CUSTOM_ELEMENTS_SCHEMA],
  template: `<arc-segmented-control [attr.value]="this.value" [disabled]="this.disabled" (arc-change)="this.onArcChange($event)"></arc-segmented-control>`,
})
export class SegmentedControl {
  @Input() value: string = '';
  @Input() disabled: boolean = false;
  @Output() arcChange = new EventEmitter<CustomEvent>();
  @Output() valueChange = new EventEmitter<string>();

  onArcChange(event: CustomEvent) {
    this.arcChange.emit(event);
    const detail = event.detail as Record<string, unknown> | null;
    if (!detail) return;
    if ('value' in detail) {
      const next = detail.value as string;
      this.value = next;
      this.valueChange.emit(next);
    }
  }
}
