// Auto-generated by @arclux/prism 3.1.1 — do not edit manually

import { Component, ElementRef, inject, Input, Output, EventEmitter } from '@angular/core';
import '@arclux/arc-ui/chip';
import type { ArcChip } from '@arclux/arc-ui/chip';

@Component({
  selector: 'arc-chip',
  standalone: true,
  template: `<ng-content />`,
  host: {
    '(arc-change)': '_onArcChange($event)',
  },
})
export class Chip {
  private readonly _el: ArcChip = inject(ElementRef).nativeElement;

  @Input() set selected(value: boolean) {
    this._el.selected = value;
  }
  get selected(): boolean {
    return this._el.selected;
  }

  @Input() set disabled(value: boolean) {
    this._el.disabled = value;
  }
  get disabled(): boolean {
    return this._el.disabled;
  }

  @Input() set value(value: string) {
    this._el.value = value;
  }
  get value(): string {
    return this._el.value;
  }

  @Output() selectedChange = new EventEmitter<boolean>();

  _onArcChange(event: CustomEvent) {
    const detail = event.detail as Record<string, unknown> | null;
    if (!detail) return;
    if ('selected' in detail) {
      this.selectedChange.emit(detail.selected as boolean);
    }
  }
}
