// Auto-generated by @arclux/prism — do not edit manually

import { Component, ElementRef, inject, Input, Output, EventEmitter } from '@angular/core';
import { ArcCalendar } from '@arclux/arc-ui/calendar';

@Component({
  selector: 'arc-calendar',
  standalone: true,
  template: ``,
  host: {
    '(arc-month-change)': '_onArcMonthChange($event)',
    '(arc-change)': '_onArcChange($event)',
  },
})
export class Calendar {
  private readonly _el: ArcCalendar = inject(ElementRef).nativeElement;

  @Input() set locale(value: string) {
    this._el.locale = value;
  }
  get locale(): string {
    return this._el.locale;
  }

  @Input() set firstDayOfWeek(value: number) {
    this._el.firstDayOfWeek = value;
  }
  get firstDayOfWeek(): number {
    return this._el.firstDayOfWeek;
  }

  @Input() set value(value: string) {
    this._el.value = value;
  }
  get value(): string {
    return this._el.value;
  }

  @Input() set min(value: string) {
    this._el.min = value;
  }
  get min(): string {
    return this._el.min;
  }

  @Input() set max(value: string) {
    this._el.max = value;
  }
  get max(): string {
    return this._el.max;
  }

  @Input() set month(value: number) {
    this._el.month = value;
  }
  get month(): number {
    return this._el.month;
  }

  @Input() set year(value: number) {
    this._el.year = value;
  }
  get year(): number {
    return this._el.year;
  }

  @Output() valueChange = new EventEmitter<string>();

  @Output() monthChange = new EventEmitter<number>();

  @Output() yearChange = new EventEmitter<number>();

  _onArcMonthChange(event: CustomEvent) {
    const detail = event.detail as Record<string, unknown> | null;
    if (!detail) return;
    if ('month' in detail) {
      this.monthChange.emit(detail.month as number);
    }
    if ('year' in detail) {
      this.yearChange.emit(detail.year as number);
    }
  }

  _onArcChange(event: CustomEvent) {
    const detail = event.detail as Record<string, unknown> | null;
    if (!detail) return;
    if ('value' in detail) {
      this.valueChange.emit(detail.value as string);
    }
  }
}
