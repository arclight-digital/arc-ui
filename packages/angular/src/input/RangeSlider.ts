// Auto-generated by @arclux/prism 3.1.1 — do not edit manually

import { Component, ElementRef, inject, Input, Output, EventEmitter, forwardRef } from '@angular/core';
import { NG_VALUE_ACCESSOR, type ControlValueAccessor } from '@angular/forms';
import '@arclux/arc-ui/range-slider';
import type { ArcRangeSlider } from '@arclux/arc-ui/range-slider';

@Component({
  selector: 'arc-range-slider',
  standalone: true,
  template: ``,
  host: {
    '(arc-input)': '_onArcInput($event)',
    '(arc-change)': '_onArcChange($event)',
  },
  providers: [
    { provide: NG_VALUE_ACCESSOR, useExisting: forwardRef(() => RangeSlider), multi: true },
  ],
})
export class RangeSlider implements ControlValueAccessor {
  private readonly _el: ArcRangeSlider = inject(ElementRef).nativeElement;

  /** The element, for the methods it is driven by: checkValidity() and reportValidity(). */
  get element(): ArcRangeSlider {
    return this._el;
  }

  @Input() set required(value: boolean) {
    this._el.required = value;
  }
  get required(): boolean {
    return this._el.required;
  }

  @Input() set readonly(value: boolean) {
    this._el.readonly = value;
  }
  get readonly(): boolean {
    return this._el.readonly;
  }

  @Input() set size(value: 'sm' | 'md' | 'lg') {
    this._el.size = value;
  }
  get size(): 'sm' | 'md' | 'lg' {
    return this._el.size;
  }

  @Input() set min(value: number) {
    this._el.min = value;
  }
  get min(): number {
    return this._el.min;
  }

  @Input() set max(value: number) {
    this._el.max = value;
  }
  get max(): number {
    return this._el.max;
  }

  @Input() set step(value: number) {
    this._el.step = value;
  }
  get step(): number {
    return this._el.step;
  }

  @Input() set low(value: number) {
    this._el.low = value;
  }
  get low(): number {
    return this._el.low;
  }

  @Input() set high(value: number) {
    this._el.high = value;
  }
  get high(): number {
    return this._el.high;
  }

  @Input() set name(value: string) {
    this._el.name = value;
  }
  get name(): string {
    return this._el.name;
  }

  @Input() set disabled(value: boolean) {
    this._el.disabled = value;
  }
  get disabled(): boolean {
    return this._el.disabled;
  }

  @Input() set label(value: string) {
    this._el.label = value;
  }
  get label(): string {
    return this._el.label;
  }

  @Input() set showValues(value: boolean) {
    this._el.showValues = value;
  }
  get showValues(): boolean {
    return this._el.showValues;
  }

  @Output() lowChange = new EventEmitter<number>();

  @Output() highChange = new EventEmitter<number>();

  _onArcInput(event: CustomEvent) {
    const detail = event.detail as Record<string, unknown> | null;
    if (!detail) return;
    if ('high' in detail) {
      this.highChange.emit(detail.high as number);
    }
    if ('low' in detail) {
      this.lowChange.emit(detail.low as number);
    }
  }

  _onArcChange(event: CustomEvent) {
    const detail = event.detail as Record<string, unknown> | null;
    if (!detail) return;
    if ('high' in detail) {
      this.highChange.emit(detail.high as number);
    }
    if ('low' in detail) {
      this.lowChange.emit(detail.low as number);
    }
  }

  /* ── ControlValueAccessor ──
     Bound to `low` / `high`, committed on `arc-change`.
     The listener lives in the constructor because the host metadata already
     maps that event to this component’s own @Output. writeValue deliberately
     does not call _onChangeFn: it is the form writing into the element, and
     echoing it back would mark the control dirty on every setValue. */
  private _onChangeFn: (value: { low: number; high: number }) => void = () => {};
  private _onTouchedFn: () => void = () => {};

  constructor() {
    this._el.addEventListener('arc-change', () => {
      this._onChangeFn({ low: this._el.low, high: this._el.high });
      this._onTouchedFn();
    });
  }

  writeValue(value: { low: number; high: number } | null | undefined): void {
    const next = value ?? { low: 0, high: 0 };
    this._el.low = next.low;
    this._el.high = next.high;
  }

  registerOnChange(fn: (value: { low: number; high: number }) => void): void {
    this._onChangeFn = fn;
  }

  registerOnTouched(fn: () => void): void {
    this._onTouchedFn = fn;
  }

  setDisabledState(isDisabled: boolean): void {
    this._el.disabled = isDisabled;
  }
}
