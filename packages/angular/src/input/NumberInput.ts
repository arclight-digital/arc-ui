// Auto-generated by @arclux/prism — do not edit manually

import { Component, CUSTOM_ELEMENTS_SCHEMA, Input, Output, EventEmitter } from '@angular/core';
import '@arclux/arc-ui/number-input';

@Component({
  selector: 'arc-number-input',
  standalone: true,
  schemas: [CUSTOM_ELEMENTS_SCHEMA],
  template: `<arc-number-input [attr.value]="this.value" [attr.min]="this.min" [attr.max]="this.max" [attr.step]="this.step" [attr.label]="this.label" [attr.name]="this.name" [disabled]="this.disabled" (arc-change)="this.onArcChange($event)"></arc-number-input>`,
})
export class NumberInput {
  @Input() value: number = 0;
  @Input() min: number = 'undefined';
  @Input() max: number = 'undefined';
  @Input() step: number = 1;
  @Input() label: string = '';
  @Input() name: string = '';
  @Input() disabled: boolean = false;
  @Output() arcChange = new EventEmitter<CustomEvent>();
  @Output() valueChange = new EventEmitter<number>();

  onArcChange(event: CustomEvent) {
    this.arcChange.emit(event);
    const detail = event.detail as Record<string, unknown> | null;
    if (!detail) return;
    if ('value' in detail) {
      const next = detail.value as number;
      this.value = next;
      this.valueChange.emit(next);
    }
  }
}
