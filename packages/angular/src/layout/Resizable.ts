// Auto-generated by @arclux/prism 3.0.0 — do not edit manually

import { Component, ElementRef, inject, Input, Output, EventEmitter } from '@angular/core';
import '@arclux/arc-ui/resizable';
import type { ArcResizable } from '@arclux/arc-ui/resizable';

@Component({
  selector: 'arc-resizable',
  standalone: true,
  template: `<ng-content />`,
  host: {
    '(arc-resize)': '_onArcResize($event)',
  },
})
export class Resizable {
  private readonly _el: ArcResizable = inject(ElementRef).nativeElement;

  @Input() set direction(value: 'horizontal' | 'vertical') {
    this._el.direction = value;
  }
  get direction(): 'horizontal' | 'vertical' {
    return this._el.direction;
  }

  @Input() set minSize(value: number) {
    this._el.minSize = value;
  }
  get minSize(): number {
    return this._el.minSize;
  }

  @Input() set maxSize(value: number) {
    this._el.maxSize = value;
  }
  get maxSize(): number {
    return this._el.maxSize;
  }

  @Input() set size(value: number) {
    this._el.size = value;
  }
  get size(): number {
    return this._el.size;
  }

  @Output() sizeChange = new EventEmitter<number>();

  _onArcResize(event: CustomEvent) {
    const detail = event.detail as Record<string, unknown> | null;
    if (!detail) return;
    if ('size' in detail) {
      this.sizeChange.emit(detail.size as number);
    }
  }
}
