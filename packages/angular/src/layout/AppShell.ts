// Auto-generated by @arclux/prism 3.1.1 — do not edit manually

import { Component, ElementRef, inject, Input, Output, EventEmitter } from '@angular/core';
import '@arclux/arc-ui/app-shell';
import type { ArcAppShell } from '@arclux/arc-ui/app-shell';

@Component({
  selector: 'arc-app-shell',
  standalone: true,
  template: `<ng-content />`,
  host: {
    '(arc-sidebar-toggle)': '_onArcSidebarToggle($event)',
  },
})
export class AppShell {
  private readonly _el: ArcAppShell = inject(ElementRef).nativeElement;

  @Input() set sidebarOpen(value: boolean) {
    this._el.sidebarOpen = value;
  }
  get sidebarOpen(): boolean {
    return this._el.sidebarOpen;
  }

  @Input() set embedded(value: boolean) {
    this._el.embedded = value;
  }
  get embedded(): boolean {
    return this._el.embedded;
  }

  @Input() set breakpoint(value: number) {
    this._el.breakpoint = value;
  }
  get breakpoint(): number {
    return this._el.breakpoint;
  }

  @Output() sidebarOpenChange = new EventEmitter<boolean>();

  _onArcSidebarToggle(event: CustomEvent) {
    const detail = event.detail as Record<string, unknown> | null;
    if (!detail) return;
    if ('value' in detail) {
      this.sidebarOpenChange.emit(detail.value as boolean);
    }
  }
}
