// Auto-generated by @arclux/prism — do not edit manually

import { Component, ElementRef, inject, Input, Output, EventEmitter } from '@angular/core';
import '@arclux/arc-ui/sticky';
import type { ArcSticky } from '@arclux/arc-ui/sticky';

@Component({
  selector: 'arc-sticky',
  standalone: true,
  template: `<ng-content />`,
  host: {
    '(arc-stuck)': '_onArcStuck($event)',
  },
})
export class Sticky {
  private readonly _el: ArcSticky = inject(ElementRef).nativeElement;

  @Input() set offset(value: string) {
    this._el.offset = value;
  }
  get offset(): string {
    return this._el.offset;
  }

  @Input() set stuck(value: boolean) {
    this._el.stuck = value;
  }
  get stuck(): boolean {
    return this._el.stuck;
  }

  @Output() stuckChange = new EventEmitter<boolean>();

  _onArcStuck(event: CustomEvent) {
    const detail = event.detail as Record<string, unknown> | null;
    if (!detail) return;
    if ('stuck' in detail) {
      this.stuckChange.emit(detail.stuck as boolean);
    }
  }
}
