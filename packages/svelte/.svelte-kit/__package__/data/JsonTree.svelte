<!-- Auto-generated by @arclux/prism — do not edit manually -->
<script lang="ts">
  import '@arclux/arc-ui/json-tree';
  import type { Snippet } from 'svelte';

  interface Props {
    data?: string;
    json?: string;
    expanded?: number | boolean;
    keysQuoted?: boolean;
    class?: string;
    id?: string;
    style?: string;
    title?: string;
    role?: string;
    slot?: string;
    part?: string;
    exportparts?: string;
    dir?: string;
    lang?: string;
    translate?: string;
    accesskey?: string;
    enterkeyhint?: string;
    inputmode?: string;
    popover?: string;
    contenteditable?: boolean | string;
    tabindex?: number;
    hidden?: boolean;
    inert?: boolean;
    draggable?: boolean;
    spellcheck?: boolean;
    autofocus?: boolean;
    [key: `data-${string}`]: unknown;
    [key: `aria-${string}`]: unknown;
    [key: `on${string}`]: unknown;
  }

  let { data, json = '', expanded = $bindable(1), keysQuoted = false, ...rest }: Props = $props();

  let __el: HTMLElement | undefined = $state();
  $effect(() => {
    const el = __el as unknown as Record<string, unknown> | undefined;
    if (!el) return;
    if (keysQuoted !== undefined) el.keysQuoted = keysQuoted;
  });

  // Two-way binding — mirror the event detail back onto the prop, then
  // forward to the consumer's own handler, which {...rest} would otherwise
  // have attached. These are declared after {...rest} below so they win.
  function __onArcToggle(e: Event) {
    const detail = (e as CustomEvent).detail as Record<string, unknown> | null;
    if (detail) {
      if ('expanded' in detail) expanded = detail.expanded as number | boolean;
    }
    (rest['onarc-toggle'] as ((e: Event) => void) | undefined)?.(e);
  }
</script>

<arc-json-tree {data} {json} {expanded} bind:this={__el} {...rest}
  onarc-toggle={__onArcToggle}
>
</arc-json-tree>
