<!-- Auto-generated by @arclux/prism 3.1.1 — do not edit manually -->
<script lang="ts">
  import '@arclux/arc-ui/image-compare';
  import type { Snippet } from 'svelte';

  interface Props {
    position?: number;
    orientation?: 'horizontal' | 'vertical';
    beforeLabel?: string;
    afterLabel?: string;
    label?: string;
    /** <slot name="after"> — put slot="after" on the element inside. */
    after?: Snippet;
    /** <slot name="before"> — put slot="before" on the element inside. */
    before?: Snippet;
    class?: string;
    id?: string;
    style?: string;
    title?: string;
    role?: string;
    slot?: string;
    part?: string;
    exportparts?: string;
    dir?: string;
    lang?: string;
    translate?: string;
    accesskey?: string;
    enterkeyhint?: string;
    inputmode?: string;
    popover?: string;
    contenteditable?: boolean | string;
    tabindex?: number;
    hidden?: boolean;
    inert?: boolean;
    draggable?: boolean;
    spellcheck?: boolean;
    autofocus?: boolean;
    [key: `data-${string}`]: unknown;
    [key: `aria-${string}`]: unknown;
    [key: `on${string}`]: unknown;
  }

  let { position = $bindable(), orientation, beforeLabel = '', afterLabel = '', label = '', after, before, ...rest }: Props = $props();

  let __el: HTMLElement | undefined = $state();
  $effect(() => {
    const el = __el as unknown as Record<string, unknown> | undefined;
    if (!el) return;
    if (beforeLabel !== undefined) el.beforeLabel = beforeLabel;
    if (afterLabel !== undefined) el.afterLabel = afterLabel;
  });

  // Two-way binding — mirror the event detail back onto the prop, then
  // forward to the consumer's own handler, which {...rest} would otherwise
  // have attached. These are declared after {...rest} below so they win.
  function __onArcInput(e: Event) {
    const detail = (e as CustomEvent).detail as Record<string, unknown> | null;
    if (detail) {
      if ('value' in detail) position = detail.value as number;
    }
    (rest['onarc-input'] as ((e: Event) => void) | undefined)?.(e);
  }
  function __onArcChange(e: Event) {
    const detail = (e as CustomEvent).detail as Record<string, unknown> | null;
    if (detail) {
      if ('value' in detail) position = detail.value as number;
    }
    (rest['onarc-change'] as ((e: Event) => void) | undefined)?.(e);
  }
</script>

<arc-image-compare {position} {orientation} {label} bind:this={__el} {...rest}
  onarc-input={__onArcInput}
  onarc-change={__onArcChange}
>
  {@render after?.()}
  {@render before?.()}
</arc-image-compare>
