<!-- Auto-generated by @arclux/prism 3.1.1 — do not edit manually -->
<script lang="ts">
  import '@arclux/arc-ui/hotspot';
  import type { ArcHotspot } from '@arclux/arc-ui/hotspot';
  import type { Snippet } from 'svelte';

  interface Props {
    x?: number;
    y?: number;
    label?: string;
    open?: boolean;
    children?: Snippet;
    class?: string;
    id?: string;
    style?: string;
    title?: string;
    role?: string;
    slot?: string;
    part?: string;
    exportparts?: string;
    dir?: string;
    lang?: string;
    translate?: string;
    accesskey?: string;
    enterkeyhint?: string;
    inputmode?: string;
    popover?: string;
    contenteditable?: boolean | string;
    tabindex?: number;
    hidden?: boolean;
    inert?: boolean;
    draggable?: boolean;
    spellcheck?: boolean;
    autofocus?: boolean;
    [key: `data-${string}`]: unknown;
    [key: `aria-${string}`]: unknown;
    [key: `on${string}`]: unknown;
  }

  let { x, y, label = '', open, children, ...rest }: Props = $props();

  let __el: HTMLElement | undefined = $state();

  /**
   * The element, for the methods it is driven by: close().
   *
   * A function rather than a property: an instance `export` in runes mode
   * captures its binding once, which would be before the element exists.
   * Reach it with `bind:this` on this component, then `element()`.
   */
  export function element(): ArcHotspot | undefined {
    return __el as ArcHotspot | undefined;
  }
</script>

<arc-hotspot {x} {y} {label} {open} bind:this={__el} {...rest}>
  {@render children?.()}
</arc-hotspot>
