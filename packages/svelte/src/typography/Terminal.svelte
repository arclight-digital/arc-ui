<!-- Auto-generated by @arclux/prism 3.1.1 — do not edit manually -->
<script lang="ts">
  import '@arclux/arc-ui/terminal';
  import type { ArcTerminal } from '@arclux/arc-ui/terminal';

  interface Props {
    lines?: unknown[];
    prompt?: string;
    title?: string;
    speed?: number;
    autoplay?: boolean;
    loop?: boolean;
    class?: string;
    id?: string;
    style?: string;
    role?: string;
    slot?: string;
    part?: string;
    exportparts?: string;
    dir?: string;
    lang?: string;
    translate?: string;
    accesskey?: string;
    enterkeyhint?: string;
    inputmode?: string;
    popover?: string;
    contenteditable?: boolean | string;
    tabindex?: number;
    hidden?: boolean;
    inert?: boolean;
    draggable?: boolean;
    spellcheck?: boolean;
    autofocus?: boolean;
    [key: `data-${string}`]: unknown;
    [key: `aria-${string}`]: unknown;
    [key: `on${string}`]: unknown;
  }

  let { lines, prompt = '$', title = '', speed = 50, autoplay, loop, ...rest }: Props = $props();

  let __el: HTMLElement | undefined = $state();

  /**
   * The element, for the methods it is driven by: play() and reset().
   *
   * A function rather than a property: an instance `export` in runes mode
   * captures its binding once, which would be before the element exists.
   * Reach it with `bind:this` on this component, then `element()`.
   */
  export function element(): ArcTerminal | undefined {
    return __el as ArcTerminal | undefined;
  }
</script>

<arc-terminal {lines} {prompt} {title} {speed} {autoplay} {loop} bind:this={__el} {...rest}>
</arc-terminal>
