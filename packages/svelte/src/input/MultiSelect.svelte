<!-- Auto-generated by @arclux/prism 3.0.0 — do not edit manually -->
<script lang="ts">
  import '@arclux/arc-ui/multi-select';
  import type { Snippet } from 'svelte';

  interface Props {
    required?: boolean;
    readonly?: boolean;
    size?: 'sm' | 'md' | 'lg';
    value?: string[];
    placeholder?: string;
    label?: string;
    name?: string;
    disabled?: boolean;
    children?: Snippet;
    class?: string;
    id?: string;
    style?: string;
    title?: string;
    role?: string;
    slot?: string;
    part?: string;
    exportparts?: string;
    dir?: string;
    lang?: string;
    translate?: string;
    accesskey?: string;
    enterkeyhint?: string;
    inputmode?: string;
    popover?: string;
    contenteditable?: boolean | string;
    tabindex?: number;
    hidden?: boolean;
    inert?: boolean;
    draggable?: boolean;
    spellcheck?: boolean;
    autofocus?: boolean;
    [key: `data-${string}`]: unknown;
    [key: `aria-${string}`]: unknown;
    [key: `on${string}`]: unknown;
  }

  let { required, readonly, size, value = $bindable(), placeholder = '', label = '', name = '', disabled = false, children, ...rest }: Props = $props();

  // Two-way binding — mirror the event detail back onto the prop, then
  // forward to the consumer's own handler, which {...rest} would otherwise
  // have attached. These are declared after {...rest} below so they win.
  function __onArcChange(e: Event) {
    const detail = (e as CustomEvent).detail as Record<string, unknown> | null;
    if (detail) {
      if ('value' in detail) value = detail.value as string[];
    }
    (rest['onarc-change'] as ((e: Event) => void) | undefined)?.(e);
  }
  function __onArcInput(e: Event) {
    const detail = (e as CustomEvent).detail as Record<string, unknown> | null;
    if (detail) {
      if ('value' in detail) value = detail.value as string[];
    }
    (rest['onarc-input'] as ((e: Event) => void) | undefined)?.(e);
  }
</script>

<arc-multi-select {required} {readonly} {size} {value} {placeholder} {label} {name} {disabled} {...rest}
  onarc-change={__onArcChange}
  onarc-input={__onArcInput}
>
  {@render children?.()}
</arc-multi-select>
