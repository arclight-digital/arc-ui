<!-- Auto-generated by @arclux/prism 3.1.1 — do not edit manually -->
<script lang="ts">
  import '@arclux/arc-ui/time-picker';
  import type { ArcTimePicker } from '@arclux/arc-ui/time-picker';

  interface Props {
    required?: boolean;
    readonly?: boolean;
    size?: 'sm' | 'md' | 'lg';
    value?: string;
    name?: string;
    min?: string;
    max?: string;
    step?: 1 | 5 | 15 | 30;
    format?: '12h' | '24h';
    placeholder?: string;
    disabled?: boolean;
    label?: string;
    open?: boolean;
    class?: string;
    id?: string;
    style?: string;
    title?: string;
    role?: string;
    slot?: string;
    part?: string;
    exportparts?: string;
    dir?: string;
    lang?: string;
    translate?: string;
    accesskey?: string;
    enterkeyhint?: string;
    inputmode?: string;
    popover?: string;
    contenteditable?: boolean | string;
    tabindex?: number;
    hidden?: boolean;
    inert?: boolean;
    draggable?: boolean;
    spellcheck?: boolean;
    autofocus?: boolean;
    [key: `data-${string}`]: unknown;
    [key: `aria-${string}`]: unknown;
    [key: `on${string}`]: unknown;
  }

  let { required, readonly, size, value = $bindable(''), name = '', min = '', max = '', step, format, placeholder = 'Select time', disabled = false, label = '', open = false, ...rest }: Props = $props();

  let __el: HTMLElement | undefined = $state();

  /**
   * The element, for the methods it is driven by: checkValidity() and reportValidity().
   *
   * A function rather than a property: an instance `export` in runes mode
   * captures its binding once, which would be before the element exists.
   * Reach it with `bind:this` on this component, then `element()`.
   */
  export function element(): ArcTimePicker | undefined {
    return __el as ArcTimePicker | undefined;
  }

  // Two-way binding — mirror the event detail back onto the prop, then
  // forward to the consumer's own handler, which {...rest} would otherwise
  // have attached. These are declared after {...rest} below so they win.
  function __onArcChange(e: Event) {
    const detail = (e as CustomEvent).detail as Record<string, unknown> | null;
    if (detail) {
      if ('value' in detail) value = detail.value as string;
    }
    (rest['onarc-change'] as ((e: Event) => void) | undefined)?.(e);
  }
</script>

<arc-time-picker {required} {readonly} {size} {value} {name} {min} {max} {step} {format} {placeholder} {disabled} {label} {open} bind:this={__el} {...rest}
  onarc-change={__onArcChange}
>
</arc-time-picker>
