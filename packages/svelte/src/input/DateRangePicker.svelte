<!-- Auto-generated by @arclux/prism — do not edit manually -->
<script lang="ts">
  import '@arclux/arc-ui/date-range-picker';
  import type { Snippet } from 'svelte';

  interface Props {
    size?: 'sm' | 'md' | 'lg';
    locale?: string;
    firstDayOfWeek?: number;
    start?: string;
    end?: string;
    name?: string;
    min?: string;
    max?: string;
    months?: number;
    presets?: Array<{label:string,days:number}>;
    placeholder?: string;
    disabled?: boolean;
    required?: boolean;
    label?: string;
    open?: boolean;
    class?: string;
    id?: string;
    style?: string;
    title?: string;
    role?: string;
    slot?: string;
    part?: string;
    exportparts?: string;
    dir?: string;
    lang?: string;
    translate?: string;
    accesskey?: string;
    enterkeyhint?: string;
    inputmode?: string;
    popover?: string;
    contenteditable?: boolean | string;
    tabindex?: number;
    hidden?: boolean;
    inert?: boolean;
    draggable?: boolean;
    spellcheck?: boolean;
    autofocus?: boolean;
    [key: `data-${string}`]: unknown;
    [key: `aria-${string}`]: unknown;
    [key: `on${string}`]: unknown;
  }

  let { size = 'md', locale = '', firstDayOfWeek = 0, start = $bindable(''), end = $bindable(''), name = '', min = '', max = '', months = 2, presets = [], placeholder = 'Select date range', disabled = false, required = false, label = '', open = false, ...rest }: Props = $props();

  let __el: HTMLElement | undefined = $state();
  $effect(() => {
    const el = __el as unknown as Record<string, unknown> | undefined;
    if (!el) return;
    if (firstDayOfWeek !== undefined) el.firstDayOfWeek = firstDayOfWeek;
  });

  // Two-way binding — mirror the event detail back onto the prop, then
  // forward to the consumer's own handler, which {...rest} would otherwise
  // have attached. These are declared after {...rest} below so they win.
  function __onArcChange(e: Event) {
    const detail = (e as CustomEvent).detail as Record<string, unknown> | null;
    if (detail) {
      if ('end' in detail) end = detail.end as string;
      if ('start' in detail) start = detail.start as string;
    }
    (rest['onarc-change'] as ((e: Event) => void) | undefined)?.(e);
  }
</script>

<arc-date-range-picker {size} {locale} {start} {end} {name} {min} {max} {months} {presets} {placeholder} {disabled} {required} {label} {open} bind:this={__el} {...rest}
  onarc-change={__onArcChange}
>
</arc-date-range-picker>
