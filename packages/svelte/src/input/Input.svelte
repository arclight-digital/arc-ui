<!-- Auto-generated by @arclux/prism — do not edit manually -->
<script lang="ts">
  import '@arclux/arc-ui/input';
  import type { Snippet } from 'svelte';

  interface Props {
    type?: string;
    name?: string;
    label?: string;
    placeholder?: string;
    value?: string;
    disabled?: boolean;
    required?: boolean;
    error?: string;
    size?: 'sm' | 'lg';
    multiline?: boolean;
    rows?: number;
    children?: Snippet;
    [key: string]: unknown;
  }

  let { type = 'text', name = '', label = '', placeholder = '', value = $bindable(''), disabled = false, required = false, error = '', size = 'md', multiline = false, rows = 5, children, ...rest }: Props = $props();

  // Two-way binding — mirror the event detail back onto the prop, then
  // forward to the consumer's own handler, which {...rest} would otherwise
  // have attached. These are declared after {...rest} below so they win.
  function __onArcInput(e: Event) {
    const detail = (e as CustomEvent).detail as Record<string, unknown> | null;
    if (detail) {
      if ('value' in detail) value = detail.value as string;
    }
    (rest['onarc-input'] as ((e: Event) => void) | undefined)?.(e);
  }
  function __onArcChange(e: Event) {
    const detail = (e as CustomEvent).detail as Record<string, unknown> | null;
    if (detail) {
      if ('value' in detail) value = detail.value as string;
    }
    (rest['onarc-change'] as ((e: Event) => void) | undefined)?.(e);
  }
</script>

<arc-input {type} {name} {label} {placeholder} {value} {disabled} {required} {error} {size} {multiline} {rows} {...rest}
  onarc-input={__onArcInput}
  onarc-change={__onArcChange}
>
  {@render children?.()}
</arc-input>
