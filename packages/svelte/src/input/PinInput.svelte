<!-- Auto-generated by @arclux/prism 3.1.1 — do not edit manually -->
<script lang="ts">
  import '@arclux/arc-ui/pin-input';
  import type { ArcPinInput } from '@arclux/arc-ui/pin-input';

  interface Props {
    required?: boolean;
    readonly?: boolean;
    size?: 'sm' | 'md' | 'lg';
    length?: number;
    value?: string;
    name?: string;
    disabled?: boolean;
    mask?: boolean;
    type?: 'number' | 'alphanumeric' | 'text';
    separator?: number;
    label?: string;
    class?: string;
    id?: string;
    style?: string;
    title?: string;
    role?: string;
    slot?: string;
    part?: string;
    exportparts?: string;
    dir?: string;
    lang?: string;
    translate?: string;
    accesskey?: string;
    enterkeyhint?: string;
    inputmode?: string;
    popover?: string;
    contenteditable?: boolean | string;
    tabindex?: number;
    hidden?: boolean;
    inert?: boolean;
    draggable?: boolean;
    spellcheck?: boolean;
    autofocus?: boolean;
    [key: `data-${string}`]: unknown;
    [key: `aria-${string}`]: unknown;
    [key: `on${string}`]: unknown;
  }

  let { required, readonly, size, length, value = $bindable(''), name = '', disabled = false, mask, type, separator = 0, label = '', ...rest }: Props = $props();

  let __el: HTMLElement | undefined = $state();

  /**
   * The element, for the methods it is driven by: checkValidity() and reportValidity().
   *
   * A function rather than a property: an instance `export` in runes mode
   * captures its binding once, which would be before the element exists.
   * Reach it with `bind:this` on this component, then `element()`.
   */
  export function element(): ArcPinInput | undefined {
    return __el as ArcPinInput | undefined;
  }

  // Two-way binding — mirror the event detail back onto the prop, then
  // forward to the consumer's own handler, which {...rest} would otherwise
  // have attached. These are declared after {...rest} below so they win.
  function __onArcInput(e: Event) {
    const detail = (e as CustomEvent).detail as Record<string, unknown> | null;
    if (detail) {
      if ('value' in detail) value = detail.value as string;
    }
    (rest['onarc-input'] as ((e: Event) => void) | undefined)?.(e);
  }
  function __onArcChange(e: Event) {
    const detail = (e as CustomEvent).detail as Record<string, unknown> | null;
    if (detail) {
      if ('value' in detail) value = detail.value as string;
    }
    (rest['onarc-change'] as ((e: Event) => void) | undefined)?.(e);
  }
  function __onArcComplete(e: Event) {
    const detail = (e as CustomEvent).detail as Record<string, unknown> | null;
    if (detail) {
      if ('value' in detail) value = detail.value as string;
    }
    (rest['onarc-complete'] as ((e: Event) => void) | undefined)?.(e);
  }
</script>

<arc-pin-input {required} {readonly} {size} {length} {value} {name} {disabled} {mask} {type} {separator} {label} bind:this={__el} {...rest}
  onarc-input={__onArcInput}
  onarc-change={__onArcChange}
  onarc-complete={__onArcComplete}
>
</arc-pin-input>
