<!-- Auto-generated by @arclux/prism 3.1.1 — do not edit manually -->
<script setup lang="ts">
import '@arclux/arc-ui/signature-pad';
import { ref } from 'vue';
import type { ArcSignaturePad } from '@arclux/arc-ui/signature-pad';

defineOptions({ name: 'SignaturePad' });

const props = withDefaults(defineProps<{
  required?: boolean;
  readonly?: boolean;
  value?: string;
  name?: string;
  label?: string;
  disabled?: boolean;
  penColor?: string;
  penWidth?: number;
}>(), {
  value: '',
  name: '',
  label: '',
  disabled: false,
  penColor: '',
});

const emit = defineEmits<{
  'arc-clear': [event: CustomEvent];
  'arc-input': [event: CustomEvent];
  'arc-change': [event: CustomEvent];
  'update:value': [value: string];
}>();

function onArcInput(payload: CustomEvent) {
  emit('arc-input', payload);
  const detail = payload.detail as Record<string, unknown> | null;
  if (detail) {
    if ('value' in detail) emit('update:value', detail.value as string);
  }
}
function onArcChange(payload: CustomEvent) {
  emit('arc-change', payload);
  const detail = payload.detail as Record<string, unknown> | null;
  if (detail) {
    if ('value' in detail) emit('update:value', detail.value as string);
  }
}

const __el = ref<ArcSignaturePad | null>(null);
/** The element, for the methods it is driven by: clear(), toDataURL(), checkValidity() and reportValidity(). */
defineExpose({ element: __el });
</script>

<template>
  <arc-signature-pad
    :required="props.required"
    :readonly="props.readonly"
    :value="props.value"
    :name="props.name"
    :label="props.label"
    :disabled="props.disabled"
    :penColor="props.penColor"
    :penWidth="props.penWidth"
    @arc-clear="(payload: CustomEvent) => emit('arc-clear', payload)"
    @arc-input="onArcInput"
    @arc-change="onArcChange"
    ref="__el"
  >
  </arc-signature-pad>
</template>
