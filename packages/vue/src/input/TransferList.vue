<!-- Auto-generated by @arclux/prism 3.1.1 — do not edit manually -->
<script setup lang="ts">
import '@arclux/arc-ui/transfer-list';
import { ref } from 'vue';
import type { ArcTransferList } from '@arclux/arc-ui/transfer-list';

defineOptions({ name: 'TransferList' });

const props = withDefaults(defineProps<{
  required?: boolean;
  readonly?: boolean;
  size?: 'sm' | 'md' | 'lg';
  options?: Array<{value:string,label:string,disabled?:boolean}>;
  value?: string[];
  name?: string;
  disabled?: boolean;
  searchable?: boolean;
  sourceLabel?: string;
  targetLabel?: string;
}>(), {
  name: '',
  disabled: false,
  sourceLabel: 'Available',
  targetLabel: 'Selected',
});

const emit = defineEmits<{
  'arc-change': [event: CustomEvent];
  'update:value': [value: string[]];
}>();

function onArcChange(payload: CustomEvent) {
  emit('arc-change', payload);
  const detail = payload.detail as Record<string, unknown> | null;
  if (detail) {
    if ('value' in detail) emit('update:value', detail.value as string[]);
  }
}

const __el = ref<ArcTransferList | null>(null);
/** The element, for the methods it is driven by: checkValidity() and reportValidity(). */
defineExpose({ element: __el });
</script>

<template>
  <arc-transfer-list
    :required="props.required"
    :readonly="props.readonly"
    :size="props.size"
    :options="props.options"
    :value="props.value"
    :name="props.name"
    :disabled="props.disabled"
    :searchable="props.searchable"
    :sourceLabel="props.sourceLabel"
    :targetLabel="props.targetLabel"
    @arc-change="onArcChange"
    ref="__el"
  >
  </arc-transfer-list>
</template>
