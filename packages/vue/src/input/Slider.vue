<!-- Auto-generated by @arclux/prism 3.1.1 — do not edit manually -->
<script setup lang="ts">
import '@arclux/arc-ui/slider';
import { ref } from 'vue';
import type { ArcSlider } from '@arclux/arc-ui/slider';

defineOptions({ name: 'Slider' });

const props = withDefaults(defineProps<{
  required?: boolean;
  readonly?: boolean;
  size?: 'sm' | 'md' | 'lg';
  value?: number;
  min?: number;
  max?: number;
  step?: number;
  name?: string;
  disabled?: boolean;
  label?: string;
}>(), {
  value: 0,
  min: 0,
  max: 100,
  step: 1,
  name: '',
  disabled: false,
  label: '',
});

const emit = defineEmits<{
  'arc-input': [event: CustomEvent];
  'arc-change': [event: CustomEvent];
  'update:value': [value: number];
}>();

function onArcInput(payload: CustomEvent) {
  emit('arc-input', payload);
  const detail = payload.detail as Record<string, unknown> | null;
  if (detail) {
    if ('value' in detail) emit('update:value', detail.value as number);
  }
}
function onArcChange(payload: CustomEvent) {
  emit('arc-change', payload);
  const detail = payload.detail as Record<string, unknown> | null;
  if (detail) {
    if ('value' in detail) emit('update:value', detail.value as number);
  }
}

const __el = ref<ArcSlider | null>(null);
/** The element, for the methods it is driven by: checkValidity() and reportValidity(). */
defineExpose({ element: __el });
</script>

<template>
  <arc-slider
    :required="props.required"
    :readonly="props.readonly"
    :size="props.size"
    :value="props.value"
    :min="props.min"
    :max="props.max"
    :step="props.step"
    :name="props.name"
    :disabled="props.disabled"
    :label="props.label"
    @arc-input="onArcInput"
    @arc-change="onArcChange"
    ref="__el"
  >
  </arc-slider>
</template>
