<!-- Auto-generated by @arclux/prism — do not edit manually -->
<script setup lang="ts">
import '@arclux/arc-ui/inline-edit';

defineOptions({ name: 'InlineEdit' });

const props = withDefaults(defineProps<{
  value?: string;
  label?: string;
  name?: string;
  placeholder?: string;
  multiline?: boolean;
  disabled?: boolean;
}>(), {
  value: '',
  label: '',
  name: '',
  placeholder: 'Empty',
  multiline: false,
  disabled: false,
});

const emit = defineEmits<{
  'arc-change': [event: CustomEvent];
  'arc-cancel': [event: CustomEvent];
  'arc-input': [event: CustomEvent];
  'update:value': [value: string];
}>();

function onArcChange(payload: CustomEvent) {
  emit('arc-change', payload);
  const detail = payload.detail as Record<string, unknown> | null;
  if (detail) {
    if ('value' in detail) emit('update:value', detail.value as string);
  }
}
function onArcCancel(payload: CustomEvent) {
  emit('arc-cancel', payload);
  const detail = payload.detail as Record<string, unknown> | null;
  if (detail) {
    if ('value' in detail) emit('update:value', detail.value as string);
  }
}
function onArcInput(payload: CustomEvent) {
  emit('arc-input', payload);
  const detail = payload.detail as Record<string, unknown> | null;
  if (detail) {
    if ('value' in detail) emit('update:value', detail.value as string);
  }
}
</script>

<template>
  <arc-inline-edit
    :value="props.value"
    :label="props.label"
    :name="props.name"
    :placeholder="props.placeholder"
    :multiline="props.multiline"
    :disabled="props.disabled"
    @arc-change="onArcChange"
    @arc-cancel="onArcCancel"
    @arc-input="onArcInput"
  >
    <slot />
  </arc-inline-edit>
</template>
