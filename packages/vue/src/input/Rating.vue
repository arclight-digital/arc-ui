<!-- Auto-generated by @arclux/prism 3.1.1 — do not edit manually -->
<script setup lang="ts">
import '@arclux/arc-ui/rating';
import { ref } from 'vue';
import type { ArcRating } from '@arclux/arc-ui/rating';

defineOptions({ name: 'Rating' });

const props = withDefaults(defineProps<{
  required?: boolean;
  readonly?: boolean;
  size?: 'sm' | 'md' | 'lg';
  value?: number;
  max?: number;
  name?: string;
  label?: string;
  disabled?: boolean;
}>(), {
  name: '',
  label: '',
  disabled: false,
});

const emit = defineEmits<{
  'arc-change': [event: CustomEvent];
  'update:value': [value: number];
}>();

function onArcChange(payload: CustomEvent) {
  emit('arc-change', payload);
  const detail = payload.detail as Record<string, unknown> | null;
  if (detail) {
    if ('value' in detail) emit('update:value', detail.value as number);
  }
}

const __el = ref<ArcRating | null>(null);
/** The element, for the methods it is driven by: checkValidity() and reportValidity(). */
defineExpose({ element: __el });
</script>

<template>
  <arc-rating
    :required="props.required"
    :readonly="props.readonly"
    :size="props.size"
    :value="props.value"
    :max="props.max"
    :name="props.name"
    :label="props.label"
    :disabled="props.disabled"
    @arc-change="onArcChange"
    ref="__el"
  >
  </arc-rating>
</template>
