<!-- Auto-generated by @arclux/prism 3.1.1 — do not edit manually -->
<script setup lang="ts">
import '@arclux/arc-ui/sidebar-section';
import { ref } from 'vue';
import type { ArcSidebarSection } from '@arclux/arc-ui/sidebar-section';

defineOptions({ name: 'SidebarSection' });

const props = withDefaults(defineProps<{
  heading?: string;
  collapsible?: boolean;
  open?: boolean;
  icon?: string;
}>(), {
  heading: '',
  icon: '',
});

const emit = defineEmits<{
  'arc-toggle': [event: CustomEvent];
  'update:open': [value: boolean];
}>();

function onArcToggle(payload: CustomEvent) {
  emit('arc-toggle', payload);
  const detail = payload.detail as Record<string, unknown> | null;
  if (detail) {
    if ('open' in detail) emit('update:open', detail.open as boolean);
  }
}

const __el = ref<ArcSidebarSection | null>(null);
/** The element, for the methods it is driven by: toggle(). */
defineExpose({ element: __el });
</script>

<template>
  <arc-sidebar-section
    :heading="props.heading"
    :collapsible="props.collapsible"
    :open="props.open"
    :icon="props.icon"
    @arc-toggle="onArcToggle"
    ref="__el"
  >
    <slot />
  </arc-sidebar-section>
</template>
