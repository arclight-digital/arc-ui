<!-- Auto-generated by @arclux/prism 3.1.1 — do not edit manually -->
<script setup lang="ts">
import '@arclux/arc-ui/pagination';

defineOptions({ name: 'Pagination' });

const props = defineProps<{
  total?: number;
  current?: number;
  siblings?: number;
  compact?: boolean;
}>();

const emit = defineEmits<{
  'arc-change': [event: CustomEvent];
  'update:current': [value: number];
}>();

function onArcChange(payload: CustomEvent) {
  emit('arc-change', payload);
  const detail = payload.detail as Record<string, unknown> | null;
  if (detail) {
    if ('value' in detail) emit('update:current', detail.value as number);
  }
}
</script>

<template>
  <arc-pagination
    :total="props.total"
    :current="props.current"
    :siblings="props.siblings"
    :compact="props.compact"
    @arc-change="onArcChange"
  >
  </arc-pagination>
</template>
