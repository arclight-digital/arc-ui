<!-- Auto-generated by @arclux/prism 3.1.1 — do not edit manually -->
<script setup lang="ts">
import '@arclux/arc-ui/stepper-nav';

defineOptions({ name: 'StepperNav' });

const props = withDefaults(defineProps<{
  steps?: Array<string>;
  active?: number;
  linear?: boolean;
}>(), {
  steps: () => ([]),
});

const emit = defineEmits<{
  'arc-change': [event: CustomEvent];
  'arc-complete': [event: CustomEvent];
  'update:active': [value: number];
}>();

function onArcChange(payload: CustomEvent) {
  emit('arc-change', payload);
  const detail = payload.detail as Record<string, unknown> | null;
  if (detail) {
    if ('value' in detail) emit('update:active', detail.value as number);
  }
}
</script>

<template>
  <arc-stepper-nav
    :steps="props.steps"
    :active="props.active"
    :linear="props.linear"
    @arc-change="onArcChange"
    @arc-complete="(payload: CustomEvent) => emit('arc-complete', payload)"
  >
    <slot />
  </arc-stepper-nav>
</template>
