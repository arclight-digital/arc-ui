<!-- Auto-generated by @arclux/prism — do not edit manually -->
<script setup lang="ts">
import '@arclux/arc-ui/video';

defineOptions({ name: 'Video' });

const props = withDefaults(defineProps<{
  src?: string;
  poster?: string;
  label?: string;
  autoplay?: boolean;
  loop?: boolean;
  muted?: boolean;
  controls?: boolean;
  preload?: 'none' | 'metadata' | 'auto';
}>(), {
  src: '',
  poster: '',
  label: '',
  controls: true,
});

const emit = defineEmits<{
  'arc-play': [event: CustomEvent];
  'arc-pause': [event: CustomEvent];
  'arc-ended': [event: CustomEvent];
}>();
</script>

<template>
  <arc-video
    :src="props.src"
    :poster="props.poster"
    :label="props.label"
    :autoplay="props.autoplay"
    :loop="props.loop"
    :muted="props.muted"
    :controls="props.controls"
    :preload="props.preload"
    @arc-play="(payload: CustomEvent) => emit('arc-play', payload)"
    @arc-pause="(payload: CustomEvent) => emit('arc-pause', payload)"
    @arc-ended="(payload: CustomEvent) => emit('arc-ended', payload)"
  >
  </arc-video>
</template>
