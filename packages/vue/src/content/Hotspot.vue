<!-- Auto-generated by @arclux/prism 3.1.1 — do not edit manually -->
<script setup lang="ts">
import '@arclux/arc-ui/hotspot';
import { ref } from 'vue';
import type { ArcHotspot } from '@arclux/arc-ui/hotspot';

defineOptions({ name: 'Hotspot' });

const props = withDefaults(defineProps<{
  x?: number;
  y?: number;
  label?: string;
  open?: boolean;
}>(), {
  label: '',
});

const emit = defineEmits<{
  'arc-open': [event: CustomEvent];
  'arc-close': [event: CustomEvent];
}>();

const __el = ref<ArcHotspot | null>(null);
/** The element, for the methods it is driven by: close(). */
defineExpose({ element: __el });
</script>

<template>
  <arc-hotspot
    :x="props.x"
    :y="props.y"
    :label="props.label"
    :open="props.open"
    @arc-open="(payload: CustomEvent) => emit('arc-open', payload)"
    @arc-close="(payload: CustomEvent) => emit('arc-close', payload)"
    ref="__el"
  >
    <slot />
  </arc-hotspot>
</template>
