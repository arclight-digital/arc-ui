<!-- Auto-generated by @arclux/prism — do not edit manually -->
<script setup lang="ts">
import '@arclux/arc-ui/json-tree';

defineOptions({ name: 'JsonTree' });

const props = withDefaults(defineProps<{
  data?: string;
  json?: string;
  expanded?: number | boolean;
  keysQuoted?: boolean;
}>(), {
  json: '',
  expanded: 1,
  keysQuoted: false,
});

const emit = defineEmits<{
  'arc-toggle': [event: CustomEvent];
  'update:expanded': [value: number | boolean];
}>();

function onArcToggle(payload: CustomEvent) {
  emit('arc-toggle', payload);
  const detail = payload.detail as Record<string, unknown> | null;
  if (detail) {
    if ('expanded' in detail) emit('update:expanded', detail.expanded as number | boolean);
  }
}
</script>

<template>
  <arc-json-tree
    :data="props.data"
    :json="props.json"
    :expanded="props.expanded"
    :keysQuoted="props.keysQuoted"
    @arc-toggle="onArcToggle"
  >
  </arc-json-tree>
</template>
