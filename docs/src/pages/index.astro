---
import BaseLayout from '../layouts/BaseLayout.astro';
import Wordmark from '../components/Wordmark.astro';
import SiteFooter from '../components/SiteFooter.astro';
import { components } from '../data/components/index';
import fs from 'node:fs';

const codeExamples = {
  react: `import { Button, Card, Input, Toggle } from '@arclux/arc-ui-react';

function Settings() {
  return (
    <Card>
      <span slot="heading">Preferences</span>
      <Input label="Display name" placeholder="Your name" />
      <Toggle label="Dark mode" checked />
      <Button variant="primary">Save Changes</Button>
    </Card>
  );
}`,
  vue: `<template>
  <ArcCard>
    <template #heading>Preferences</template>
    <ArcInput label="Display name" placeholder="Your name" />
    <ArcToggle label="Dark mode" :checked="true" />
    <ArcButton variant="primary">Save Changes</ArcButton>
  </ArcCard>
</template>

<script setup>
import { ArcButton, ArcCard, ArcInput, ArcToggle } from '@arclux/arc-ui-vue';
</script>`,
  svelte: `<script>
  import { ArcButton, ArcCard, ArcInput, ArcToggle } from '@arclux/arc-ui-svelte';
</script>

<ArcCard>
  <span slot="heading">Preferences</span>
  <ArcInput label="Display name" placeholder="Your name" />
  <ArcToggle label="Dark mode" checked />
  <ArcButton variant="primary">Save Changes</ArcButton>
</ArcCard>`,
  html: `<arc-card>
  <span slot="heading">Preferences</span>
  <arc-input label="Display name" placeholder="Your name"></arc-input>
  <arc-toggle label="Dark mode" checked></arc-toggle>
  <arc-button variant="primary">Save Changes</arc-button>
</arc-card>

<script type="module">
  import '@arclux/arc-ui/register';
</script>`
};

const pkg = JSON.parse(fs.readFileSync(new URL('../../../packages/web-components/package.json', import.meta.url), 'utf-8'));
---

<BaseLayout>
  <!-- Top Bar -->
  <div class="theme-fixed">
    <arc-top-bar fixed nav-align="left" mobile-menu="nav">
      <div slot="logo">
        <Wordmark />
      </div>
      <arc-navigation-menu slot="center">
        <arc-nav-item href="/docs/getting-started">Docs</arc-nav-item>
        <arc-nav-item href="/docs/components">Components</arc-nav-item>
        <arc-nav-item href="/docs/tokens">Tokens</arc-nav-item>
        <arc-nav-item href="/docs/theme-synthesizer">Synthesizer</arc-nav-item>
      </arc-navigation-menu>
      <div slot="actions" class="topbar-actions">
        <arc-badge variant="primary" class="topbar-version">v{pkg.version}</arc-badge>
        <arc-theme-toggle icon-only></arc-theme-toggle>
        <arc-icon-button href="https://github.com/Arclight-Digital/arc-ui" name="github-logo" label="GitHub" variant="ghost" size="sm"></arc-icon-button>
      </div>
    </arc-top-bar>
  </div>

  <!-- Hero -->
  <header class="hero">
    <canvas class="hero__canvas" id="hero-canvas"></canvas>
    <div class="hero__dots"></div>
    <div class="hero__ambient"></div>
    <div class="hero__content">
      <a href="/docs/changelog" class="hero__announcement">
        <span class="hero__announcement-badge">v2.0</span>
        <span class="hero__announcement-text">ARC UI 2.0 is here</span>
        <svg class="hero__announcement-arrow" width="14" height="14" viewBox="0 0 16 16" fill="none"><path d="M3 8h10M9 4l4 4-4 4" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/></svg>
      </a>
      <h1 class="hero__headline">
        <span class="hero__headline-top">Components that</span>
        <span class="hero__headline-glow-wrap">
          <span class="hero__headline-glow">glow.</span>
          <span class="hero__headline-glow-shadow" aria-hidden="true">glow.</span>
        </span>
      </h1>
      <p class="hero__body">
        {components.length} production-ready UI components built on Lit. One source of truth,
        seven framework targets — React, Vue, Svelte, Angular, Solid, Preact, and vanilla JS.
      </p>
      <arc-code-block variant="basic" language="bash" code="npm install @arclux/arc-ui lit"></arc-code-block>
      <div class="hero__actions">
        <arc-button href="/docs/getting-started" variant="primary" size="md">
          Get Started
          <svg slot="suffix" width="16" height="16" viewBox="0 0 16 16" fill="none"><path d="M3 8h10M9 4l4 4-4 4" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/></svg>
        </arc-button>
        <arc-button href="/docs/components" variant="secondary" size="md">Explore Components</arc-button>
      </div>
      <div class="hero__accent-line"></div>
    </div>
  </header>

  <main>
    <arc-divider></arc-divider>

    <!-- What's New in v2 -->
    <section class="whats-new">
      <div class="whats-new__inner">
        <div class="whats-new__header reveal">
          <span class="section-label">What's New</span>
          <h2 class="section-title">Version 2.0 highlights.</h2>
        </div>
        <div class="whats-new__grid">
          <div class="whats-new__card reveal">
            <div class="whats-new__icon">
              <svg width="24" height="24" viewBox="0 0 24 24" fill="none"><path d="M12 2L2 7l10 5 10-5-10-5zM2 17l10 5 10-5M2 12l10 5 10-5" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/></svg>
            </div>
            <h3 class="whats-new__title">14 New Components</h3>
            <p class="whats-new__desc">Sparkline, Diff, Comparison, Countdown, and more — purpose-built for data-rich interfaces.</p>
          </div>
          <div class="whats-new__card reveal">
            <div class="whats-new__icon">
              <svg width="24" height="24" viewBox="0 0 24 24" fill="none"><circle cx="12" cy="12" r="10" stroke="currentColor" stroke-width="1.5"/><path d="M12 6v6l4 2" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/></svg>
            </div>
            <h3 class="whats-new__title">Theme Presets</h3>
            <p class="whats-new__desc">Ship with built-in themes or generate your own with the Theme Synthesizer.</p>
          </div>
          <div class="whats-new__card reveal">
            <div class="whats-new__icon">
              <svg width="24" height="24" viewBox="0 0 24 24" fill="none"><path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/></svg>
            </div>
            <h3 class="whats-new__title">Semantic Tokens</h3>
            <p class="whats-new__desc">160+ design tokens with semantic naming — override two base tokens to retheme everything.</p>
          </div>
          <div class="whats-new__card reveal">
            <div class="whats-new__icon">
              <svg width="24" height="24" viewBox="0 0 24 24" fill="none"><path d="M13 2L3 14h9l-1 8 10-12h-9l1-8z" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/></svg>
            </div>
            <h3 class="whats-new__title">Visual Elevation</h3>
            <p class="whats-new__desc">Glow effects, ambient gradients, and smooth transitions — every component feels alive.</p>
          </div>
        </div>
        <div class="whats-new__cta reveal">
          <arc-button href="/docs/changelog" variant="ghost" size="sm">
            Full changelog
            <svg slot="suffix" width="14" height="14" viewBox="0 0 16 16" fill="none"><path d="M3 8h10M9 4l4 4-4 4" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/></svg>
          </arc-button>
        </div>
      </div>
    </section>

    <!-- Stats -->
    <arc-divider variant="glow"></arc-divider>
    <section class="stats">
      <div class="stats__ambient"></div>
      <div class="stats__grid">
        <div class="stat-custom reveal">
          <arc-animated-number class="stat-custom__number" value="0" data-target={`${components.length}`}></arc-animated-number>
          <div class="stat-custom__rule"></div>
          <span class="stat-custom__label">Components</span>
        </div>
        <div class="stat-custom reveal">
          <arc-animated-number class="stat-custom__number" value="0" data-target="7"></arc-animated-number>
          <div class="stat-custom__rule"></div>
          <span class="stat-custom__label">Frameworks</span>
        </div>
        <div class="stat-custom reveal">
          <arc-animated-number class="stat-custom__number" value="0" data-target="0"></arc-animated-number>
          <div class="stat-custom__rule"></div>
          <span class="stat-custom__label">Dependencies</span>
        </div>
        <div class="stat-custom reveal">
          <arc-animated-number class="stat-custom__number" value="0" data-target="160"></arc-animated-number>
          <div class="stat-custom__rule"></div>
          <span class="stat-custom__label">Design Tokens</span>
        </div>
      </div>
    </section>
    <arc-divider variant="glow"></arc-divider>

    <!-- Features -->
    <section class="features">
      <div class="features__inner">
        <div class="features__header reveal">
          <span class="section-label">Why ARC UI</span>
          <h2 class="section-title">Built different.</h2>
          <p class="section-desc">Lit web components at the core, a custom code generator for every framework, zero-config theming.</p>
        </div>

        <div class="features__grid">
          <arc-feature-card heading="Web Components First" description="Built on Lit, running on web standards. No proprietary abstractions — just custom elements that work everywhere." class="reveal">
            <svg slot="icon" width="24" height="24" viewBox="0 0 24 24" fill="none"><path d="M12 2L2 7l10 5 10-5-10-5zM2 17l10 5 10-5M2 12l10 5 10-5" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/></svg>
          </arc-feature-card>

          <arc-feature-card heading="Every Framework" description="React, Vue, Svelte, Angular, Solid, Preact — we built a custom code generator (Prism) that turns Lit components into typed, native wrappers for each framework." class="reveal">
            <svg slot="icon" width="24" height="24" viewBox="0 0 24 24" fill="none"><path d="M4 15s1-1 4-1 5 2 8 2 4-1 4-1V3s-1 1-4 1-5-2-8-2-4 1-4 1z" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/><line x1="4" y1="22" x2="4" y2="15" stroke="currentColor" stroke-width="1.5"/></svg>
          </arc-feature-card>

          <arc-feature-card heading="Zero Config Theming" description="Dark mode, light mode, auto mode. Override any token with CSS custom properties. Ship a custom theme in minutes." class="reveal">
            <svg slot="icon" width="24" height="24" viewBox="0 0 24 24" fill="none"><circle cx="12" cy="12" r="10" stroke="currentColor" stroke-width="1.5"/><path d="M12 6v6l4 2" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/></svg>
          </arc-feature-card>

          <arc-feature-card heading="No Dependencies" description="Only Lit runtime. No bloated dependency trees, no version conflicts, no supply chain risk. Just your components." class="reveal">
            <svg slot="icon" width="24" height="24" viewBox="0 0 24 24" fill="none"><path d="M13 2L3 14h9l-1 8 10-12h-9l1-8z" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/></svg>
          </arc-feature-card>

          <arc-feature-card heading="Standalone CSS" description="Need zero JS? Use the HTML/CSS package for pure class-based components — perfect for server-rendered pages." class="reveal">
            <svg slot="icon" width="24" height="24" viewBox="0 0 24 24" fill="none"><rect x="2" y="3" width="20" height="14" rx="2" stroke="currentColor" stroke-width="1.5"/><path d="M8 21h8M12 17v4" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/></svg>
          </arc-feature-card>

          <arc-feature-card heading="Accessible" description="WCAG 2.1 AA. ARIA roles, keyboard navigation, focus management, and reduced motion — all built in." class="reveal">
            <svg slot="icon" width="24" height="24" viewBox="0 0 24 24" fill="none"><path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/></svg>
          </arc-feature-card>
        </div>
      </div>
    </section>

    <!-- Live Showcase -->
    <section class="showcase-section">
      <div class="showcase-section__inner">
        <div class="showcase-section__header reveal">
          <span class="section-label">Live Demo</span>
          <h2 class="section-title">See it in action.</h2>
          <p class="section-desc">Every component below is a real ARC UI web component, rendering live on this page.</p>
        </div>

        <div class="showcase-grid">
          <!-- 1. Buttons -->
          <div class="showcase-card reveal">
            <span class="showcase-card__label">Buttons</span>
            <div class="showcase-card__body">
              <arc-button variant="primary" size="sm">Primary</arc-button>
              <arc-button variant="secondary" size="sm">Secondary</arc-button>
              <arc-button variant="ghost" size="sm">Ghost</arc-button>
              <arc-button variant="outline" size="sm">Outline</arc-button>
              <arc-button variant="link" size="sm">Link</arc-button>
            </div>
          </div>

          <!-- 2. Badges + Tags -->
          <div class="showcase-card reveal">
            <span class="showcase-card__label">Badges &amp; Tags</span>
            <div class="showcase-card__body">
              <arc-badge variant="primary">Primary</arc-badge>
              <arc-badge variant="success">Success</arc-badge>
              <arc-badge variant="warning">Warning</arc-badge>
              <arc-badge variant="error">Error</arc-badge>
              <arc-tag>React</arc-tag>
              <arc-tag>Vue</arc-tag>
              <arc-tag>Svelte</arc-tag>
            </div>
          </div>

          <!-- 3. Inputs -->
          <div class="showcase-card reveal">
            <span class="showcase-card__label">Inputs</span>
            <div class="showcase-card__body showcase-card__body--stack">
              <arc-input label="Email" placeholder="you@example.com" size="sm"></arc-input>
              <arc-select label="Framework" size="sm">
                <arc-option value="react">React</arc-option>
                <arc-option value="vue">Vue</arc-option>
                <arc-option value="svelte">Svelte</arc-option>
              </arc-select>
              <arc-slider label="Volume" value="65" size="sm"></arc-slider>
            </div>
          </div>

          <!-- 4. Feedback -->
          <div class="showcase-card reveal">
            <span class="showcase-card__label">Feedback</span>
            <div class="showcase-card__body showcase-card__body--stack">
              <arc-alert variant="info" size="sm">Informational alert</arc-alert>
              <arc-alert variant="success" size="sm">Operation successful</arc-alert>
              <arc-progress value="72" label="Upload progress"></arc-progress>
            </div>
          </div>

          <!-- 5. Toggles & Checks -->
          <div class="showcase-card reveal">
            <span class="showcase-card__label">Toggles &amp; Checks</span>
            <div class="showcase-card__body showcase-card__body--stack">
              <arc-toggle label="Email notifications" checked></arc-toggle>
              <arc-toggle label="Push notifications"></arc-toggle>
              <arc-checkbox label="Enable dark mode" checked></arc-checkbox>
              <arc-checkbox label="Auto-save drafts"></arc-checkbox>
            </div>
          </div>

          <!-- 6. Navigation -->
          <div class="showcase-card reveal">
            <span class="showcase-card__label">Navigation</span>
            <div class="showcase-card__body showcase-card__body--stack">
              <arc-tabs variant="underline" selected="0">
                <arc-tab label="Overview">Overview content</arc-tab>
                <arc-tab label="Settings">Settings content</arc-tab>
                <arc-tab label="Activity">Activity content</arc-tab>
              </arc-tabs>
              <arc-breadcrumb>
                <arc-breadcrumb-item href="#">Home</arc-breadcrumb-item>
                <arc-breadcrumb-item href="#">Docs</arc-breadcrumb-item>
                <arc-breadcrumb-item>Components</arc-breadcrumb-item>
              </arc-breadcrumb>
            </div>
          </div>

          <!-- 7. Data -->
          <div class="showcase-card reveal">
            <span class="showcase-card__label">Data</span>
            <div class="showcase-card__body showcase-card__body--stack">
              <arc-avatar-group>
                <arc-avatar initials="AB" size="sm"></arc-avatar>
                <arc-avatar initials="CD" size="sm"></arc-avatar>
                <arc-avatar initials="EF" size="sm"></arc-avatar>
                <arc-avatar initials="GH" size="sm"></arc-avatar>
                <arc-avatar initials="+3" size="sm"></arc-avatar>
              </arc-avatar-group>
              <arc-stepper active="1">
                <arc-step label="Order placed"></arc-step>
                <arc-step label="Processing"></arc-step>
                <arc-step label="Shipped"></arc-step>
              </arc-stepper>
            </div>
          </div>

          <!-- 8. Accordion -->
          <div class="showcase-card reveal">
            <span class="showcase-card__label">Accordion</span>
            <div class="showcase-card__body showcase-card__body--stack" style="padding:0;">
              <arc-accordion>
                <arc-accordion-item question="Getting Started">Install with npm or use the CDN for instant setup.</arc-accordion-item>
                <arc-accordion-item question="Theming">Override CSS custom properties to match your brand.</arc-accordion-item>
                <arc-accordion-item question="Accessibility">WCAG 2.1 AA compliant with full keyboard support.</arc-accordion-item>
              </arc-accordion>
            </div>
          </div>
        </div>

        <div class="showcase-see-all reveal">
          <arc-button href="/docs/components" variant="ghost" size="sm">
            See all {components.length} components
            <svg slot="suffix" width="14" height="14" viewBox="0 0 16 16" fill="none"><path d="M3 8h10M9 4l4 4-4 4" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/></svg>
          </arc-button>
        </div>
      </div>
    </section>

    <!-- Code Example -->
    <section class="code-section">
      <div class="code-section__inner">
        <div class="code-section__text reveal">
          <span class="section-label">Developer Experience</span>
          <h2 class="section-title">Ship in minutes.</h2>
          <p class="section-desc">
            Import, use, done. Every component is self-contained with styles, accessibility,
            and theming built in. No configuration files, no global CSS conflicts, no boilerplate.
          </p>
          <div class="code-section__features">
            <div class="code-feature">
              <svg width="16" height="16" viewBox="0 0 16 16" fill="none"><path d="M13.5 4.5L6 12 2.5 8.5" stroke="var(--color-success)" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/></svg>
              <span>TypeScript types included</span>
            </div>
            <div class="code-feature">
              <svg width="16" height="16" viewBox="0 0 16 16" fill="none"><path d="M13.5 4.5L6 12 2.5 8.5" stroke="var(--color-success)" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/></svg>
              <span>Tree-shakeable imports</span>
            </div>
            <div class="code-feature">
              <svg width="16" height="16" viewBox="0 0 16 16" fill="none"><path d="M13.5 4.5L6 12 2.5 8.5" stroke="var(--color-success)" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/></svg>
              <span>SSR compatible</span>
            </div>
            <div class="code-feature">
              <svg width="16" height="16" viewBox="0 0 16 16" fill="none"><path d="M13.5 4.5L6 12 2.5 8.5" stroke="var(--color-success)" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/></svg>
              <span>CDN ready — no build step</span>
            </div>
          </div>
        </div>
        <div class="code-section__code reveal">
          <div class="code-tabs">
            <arc-tabs variant="pills" selected="0">
              <arc-tab label="React">
                <arc-code-block variant="window" filename="App.jsx" language="jsx" code={codeExamples.react}></arc-code-block>
              </arc-tab>
              <arc-tab label="Vue">
                <arc-code-block variant="window" filename="App.vue" language="html" code={codeExamples.vue}></arc-code-block>
              </arc-tab>
              <arc-tab label="Svelte">
                <arc-code-block variant="window" filename="App.svelte" language="html" code={codeExamples.svelte}></arc-code-block>
              </arc-tab>
              <arc-tab label="HTML">
                <arc-code-block variant="window" filename="index.html" language="html" code={codeExamples.html}></arc-code-block>
              </arc-tab>
            </arc-tabs>
          </div>
        </div>
      </div>
    </section>

    <!-- Framework Strip / Prism Visual -->
    <section class="frameworks-strip">
      <div class="frameworks-strip__inner">
        <span class="section-label">Prism</span>
        <h2 class="section-title" style="margin-top: var(--space-xs);">One source. Seven targets.</h2>
        <div class="prism-visual reveal">
          <div class="prism-source">
            <svg width="18" height="18" viewBox="0 0 24 24" fill="none"><path d="M12 2L2 7l10 5 10-5-10-5zM2 17l10 5 10-5M2 12l10 5 10-5" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/></svg>
            <span>Lit Web Component</span>
          </div>
          <div class="prism-arrow">
            <svg width="40" height="24" viewBox="0 0 40 24" fill="none"><path d="M2 12h32M28 6l6 6-6 6" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/></svg>
          </div>
          <div class="prism-targets">
            <span class="prism-target">React</span>
            <span class="prism-target">Vue</span>
            <span class="prism-target">Svelte</span>
            <span class="prism-target">Angular</span>
            <span class="prism-target">Solid</span>
            <span class="prism-target">Preact</span>
            <span class="prism-target">HTML / CSS</span>
          </div>
        </div>
      </div>
    </section>

    <!-- CTA -->
    <arc-cta-banner eyebrow="Ready to build?" headline="Start shipping with ARC UI." class="reveal">
      <p>
        Install in seconds. Build in minutes. Ship production-ready interfaces that look
        incredible out of the box.
      </p>
      <arc-button slot="actions" href="/docs/getting-started" variant="primary" size="md">
        Get Started
        <svg slot="suffix" width="16" height="16" viewBox="0 0 16 16" fill="none"><path d="M3 8h10M9 4l4 4-4 4" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/></svg>
      </arc-button>
      <arc-button slot="actions" href="https://github.com/Arclight-Digital/arc-ui" variant="secondary" size="md">
        <svg slot="prefix" width="16" height="16" viewBox="0 0 16 16" fill="currentColor"><path d="M8 0C3.58 0 0 3.58 0 8c0 3.54 2.29 6.53 5.47 7.59.4.07.55-.17.55-.38 0-.19-.01-.82-.01-1.49-2.01.37-2.53-.49-2.69-.94-.09-.23-.48-.94-.82-1.13-.28-.15-.68-.52-.01-.53.63-.01 1.08.58 1.23.82.72 1.21 1.87.87 2.33.66.07-.52.28-.87.51-1.07-1.78-.2-3.64-.89-3.64-3.95 0-.87.31-1.59.82-2.15-.08-.2-.36-1.02.08-2.12 0 0 .67-.21 2.2.82.64-.18 1.32-.27 2-.27.68 0 1.36.09 2 .27 1.53-1.04 2.2-.82 2.2-.82.44 1.1.16 1.92.08 2.12.51.56.82 1.27.82 2.15 0 3.07-1.87 3.75-3.65 3.95.29.25.54.73.54 1.48 0 1.07-.01 1.93-.01 2.2 0 .21.15.46.55.38A8.013 8.013 0 0016 8c0-4.42-3.58-8-8-8z"/></svg>
        View on GitHub
      </arc-button>
    </arc-cta-banner>

    <SiteFooter />
  </main>
  <arc-scroll-to-top></arc-scroll-to-top>
</BaseLayout>

<script>
  /* ── Particle Canvas ── */
  function initCanvas() {
    const canvas = document.getElementById('hero-canvas') as HTMLCanvasElement;
    if (!canvas) return;
    const ctx = canvas.getContext('2d');
    if (!ctx) return;

    const prefersReducedMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches;
    if (prefersReducedMotion) return;

    let width = 0;
    let height = 0;
    let animId: number;

    interface Particle {
      x: number; y: number;
      vx: number; vy: number;
      r: number;
      color: string;
    }

    const PARTICLE_COUNT = 50;
    const CONNECTION_DIST = 140;
    const particles: Particle[] = [];
    const colors = ['77,126,247', '139,92,246'];

    function resize() {
      const rect = canvas.parentElement!.getBoundingClientRect();
      const dpr = Math.min(window.devicePixelRatio, 2);
      const oldW = width;
      const oldH = height;
      width = rect.width;
      height = rect.height;
      canvas.width = width * dpr;
      canvas.height = height * dpr;
      canvas.style.width = width + 'px';
      canvas.style.height = height + 'px';
      ctx!.setTransform(dpr, 0, 0, dpr, 0, 0);

      // Rescale existing particles instead of regenerating
      if (oldW && oldH && particles.length) {
        const sx = width / oldW;
        const sy = height / oldH;
        for (const p of particles) {
          p.x *= sx;
          p.y *= sy;
        }
      }
    }

    function spawn() {
      particles.length = 0;
      for (let i = 0; i < PARTICLE_COUNT; i++) {
        particles.push({
          x: Math.random() * width,
          y: Math.random() * height,
          vx: (Math.random() - 0.5) * 0.4,
          vy: (Math.random() - 0.5) * 0.4,
          r: Math.random() * 1.5 + 0.5,
          color: colors[Math.floor(Math.random() * colors.length)],
        });
      }
    }

    function draw() {
      ctx!.clearRect(0, 0, width, height);

      // Connections
      for (let i = 0; i < particles.length; i++) {
        for (let j = i + 1; j < particles.length; j++) {
          const dx = particles[i].x - particles[j].x;
          const dy = particles[i].y - particles[j].y;
          const dist = Math.sqrt(dx * dx + dy * dy);
          if (dist < CONNECTION_DIST) {
            const alpha = (1 - dist / CONNECTION_DIST) * 0.12;
            ctx!.strokeStyle = `rgba(${particles[i].color},${alpha})`;
            ctx!.lineWidth = 0.5;
            ctx!.beginPath();
            ctx!.moveTo(particles[i].x, particles[i].y);
            ctx!.lineTo(particles[j].x, particles[j].y);
            ctx!.stroke();
          }
        }
      }

      // Particles
      for (const p of particles) {
        // Glow
        const grad = ctx!.createRadialGradient(p.x, p.y, 0, p.x, p.y, p.r * 8);
        grad.addColorStop(0, `rgba(${p.color},0.3)`);
        grad.addColorStop(1, `rgba(${p.color},0)`);
        ctx!.fillStyle = grad;
        ctx!.fillRect(p.x - p.r * 8, p.y - p.r * 8, p.r * 16, p.r * 16);

        // Dot
        ctx!.beginPath();
        ctx!.arc(p.x, p.y, p.r, 0, Math.PI * 2);
        ctx!.fillStyle = `rgba(${p.color},0.8)`;
        ctx!.fill();

        // Move
        p.x += p.vx;
        p.y += p.vy;
        if (p.x < 0) p.x = width;
        if (p.x > width) p.x = 0;
        if (p.y < 0) p.y = height;
        if (p.y > height) p.y = 0;
      }

      animId = requestAnimationFrame(draw);
    }

    resize();
    spawn();
    draw();

    window.addEventListener('resize', resize);

    // Cleanup on navigation
    document.addEventListener('astro:before-swap', () => {
      cancelAnimationFrame(animId);
    });
  }

  /* ── Reveal on scroll ── */
  function initReveal() {
    const els = document.querySelectorAll('.reveal');
    if (!els.length) return;

    const observer = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          entry.target.classList.add('is-visible');
          observer.unobserve(entry.target);
        }
      });
    }, { threshold: 0.1, rootMargin: '0px 0px -40px 0px' });

    els.forEach(el => observer.observe(el));
  }

  /* ── Stat count-up ── */
  function initStatCountUp() {
    const nums = document.querySelectorAll('arc-animated-number[data-target]');
    if (!nums.length) return;

    const prefersReducedMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches;

    const observer = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          const el = entry.target as any;
          const target = el.dataset.target;
          if (prefersReducedMotion) {
            el.value = target;
          } else {
            // Small delay for visual effect
            requestAnimationFrame(() => { el.value = target; });
          }
          observer.unobserve(el);
        }
      });
    }, { threshold: 0.3 });

    nums.forEach(el => observer.observe(el));
  }

  async function initNavigation() {
    const { navigate } = await import('astro:transitions/client');
    document.addEventListener('arc-navigate', (e: any) => {
      const href = e.detail?.href;
      if (href) {
        e.preventDefault();
        navigate(href);
      }
    });
  }

  initCanvas();
  initReveal();
  initStatCountUp();
  initNavigation();
  document.addEventListener('astro:page-load', () => {
    initCanvas();
    initReveal();
    initStatCountUp();
    initNavigation();
  });
</script>

<style>
  .topbar-actions { display: flex; align-items: center; gap: var(--space-sm); }

  /* ── Hero ── */
  .hero {
    position: relative;
    overflow: clip;
    padding: calc(var(--space-3xl) + 80px) var(--space-lg) var(--space-2xl);
    min-height: 85vh;
    display: flex;
    align-items: center;
  }

  .hero__canvas {
    position: absolute;
    inset: 0;
    pointer-events: none;
    z-index: 0;
  }

  .hero__dots {
    position: absolute;
    inset: 0;
    background-image: radial-gradient(circle, rgba(255,255,255,0.12) 1px, transparent 1px);
    background-size: 24px 24px;
    pointer-events: none;
    z-index: 0;
    mask-image: radial-gradient(ellipse 70% 60% at 50% 40%, black 20%, transparent 70%);
    -webkit-mask-image: radial-gradient(ellipse 70% 60% at 50% 40%, black 20%, transparent 70%);
  }

  .hero__ambient {
    position: absolute;
    inset: 0;
    background:
      radial-gradient(ellipse 80% 60% at 20% 40%, rgba(var(--accent-primary-rgb),0.18), transparent 70%),
      radial-gradient(ellipse 60% 80% at 80% 60%, rgba(var(--accent-secondary-rgb),0.14), transparent 70%),
      radial-gradient(circle at 50% 100%, rgba(var(--accent-primary-rgb),0.08), transparent 40%);
    pointer-events: none;
    z-index: 1;
  }

  .hero__content {
    position: relative;
    z-index: 2;
    max-width: var(--max-width);
    margin-inline: auto;
    width: 100%;
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    gap: var(--space-md);
  }

  /* ── Announcement Pill ── */
  .hero__announcement {
    display: inline-flex;
    align-items: center;
    gap: var(--space-xs);
    padding: 4px 14px 4px 4px;
    border-radius: var(--radius-full);
    border: 1px solid rgba(var(--accent-primary-rgb), 0.3);
    background: rgba(var(--accent-primary-rgb), 0.08);
    color: var(--text-secondary);
    font-family: var(--font-body);
    font-size: var(--text-sm);
    font-weight: 500;
    text-decoration: none;
    transition: border-color var(--transition-fast), background var(--transition-fast), box-shadow var(--transition-fast);
    position: relative;
    overflow: hidden;
    cursor: pointer;
  }

  .hero__announcement::before {
    content: '';
    position: absolute;
    inset: 0;
    background: linear-gradient(
      105deg,
      transparent 20%,
      rgba(var(--accent-primary-rgb), 0.12) 40%,
      rgba(var(--accent-secondary-rgb), 0.12) 60%,
      transparent 80%
    );
    background-size: 200% 100%;
    animation: announcement-shimmer 5s ease-in-out infinite;
  }

  @keyframes announcement-shimmer {
    0%, 100% { background-position: 200% 0; }
    50% { background-position: -200% 0; }
  }

  .hero__announcement:hover {
    border-color: rgba(var(--accent-primary-rgb), 0.5);
    background: rgba(var(--accent-primary-rgb), 0.12);
    box-shadow: 0 0 20px rgba(var(--accent-primary-rgb), 0.15), 0 0 40px rgba(var(--accent-secondary-rgb), 0.08);
  }

  .hero__announcement-badge {
    position: relative;
    display: inline-block;
    padding: 2px 8px;
    border-radius: var(--radius-full);
    background: var(--gradient-accent-text);
    color: white;
    font-size: var(--text-xs);
    font-weight: 700;
    letter-spacing: 0.5px;
    line-height: 1.4;
  }

  .hero__announcement-text {
    position: relative;
  }

  .hero__announcement-arrow {
    position: relative;
    transition: transform var(--transition-fast);
    flex-shrink: 0;
  }

  .hero__announcement:hover .hero__announcement-arrow {
    transform: translateX(2px);
  }

  @media (prefers-reduced-motion: reduce) {
    .hero__announcement::before {
      animation: none;
    }
  }

  .hero__headline {
    display: flex;
    flex-direction: column;
    gap: 0;
    line-height: 1;
  }

  .hero__headline-top {
    font-size: clamp(36px, 6vw, 60px);
    font-weight: 200;
    letter-spacing: -1px;
    color: var(--text-primary);
  }

  .hero__headline-glow-wrap {
    position: relative;
    display: block;
    padding-bottom: 12px;
  }

  .hero__headline-glow {
    position: relative;
    font-size: clamp(56px, 10vw, 96px);
    font-weight: 600;
    letter-spacing: -3px;
    background: linear-gradient(135deg, rgb(255,255,255) 0%, var(--accent-primary) 50%, var(--accent-secondary) 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    z-index: 1;
  }

  .hero__headline-glow-shadow {
    position: absolute;
    inset: 0;
    font-size: clamp(56px, 10vw, 96px);
    font-weight: 600;
    letter-spacing: -3px;
    color: rgba(var(--accent-primary-rgb), 0.35);
    filter: blur(30px);
    z-index: 0;
    pointer-events: none;
    user-select: none;
  }

  .hero__body {
    color: var(--text-secondary);
    max-width: 560px;
    text-align: center;
    text-wrap: balance;
    font-size: var(--text-md);
    line-height: 1.7;
  }

  .hero__actions {
    display: flex;
    gap: var(--space-lg);
    margin-top: var(--space-sm);
  }

  .hero__accent-line {
    width: 280px;
    height: 1px;
    background: linear-gradient(90deg, transparent, var(--accent-primary), var(--accent-secondary), transparent);
    margin-top: var(--space-md);
    filter: drop-shadow(0 0 8px rgba(var(--accent-primary-rgb),0.4));
  }

  /* ── Reveal Animations ── */
  .reveal {
    opacity: 0;
    transform: translateY(20px);
    transition: opacity 600ms var(--ease-out-expo), transform 600ms var(--ease-out-expo);
  }

  .reveal.is-visible {
    opacity: 1;
    transform: translateY(0);
  }

  @media (prefers-reduced-motion: reduce) {
    .reveal { opacity: 1; transform: none; transition: none; }
  }

  /* ── Section Labels ── */
  .section-label {
    font-family: var(--font-accent);
    font-weight: 600;
    font-size: var(--text-xs);
    letter-spacing: 4px;
    text-transform: uppercase;
    background: var(--gradient-accent-text);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .section-title {
    font-size: clamp(28px, 4vw, 40px);
    font-weight: 500;
    letter-spacing: -1px;
    background: var(--gradient-display-text);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    margin-top: var(--space-sm);
  }

  .section-desc {
    color: var(--text-secondary);
    font-size: var(--text-md);
    max-width: 560px;
    text-wrap: balance;
    line-height: 1.7;
  }

  /* ── What's New ── */
  .whats-new {
    position: relative;
    padding: var(--space-3xl) 0 var(--space-2xl);
  }

  .whats-new::after {
    content: '';
    position: absolute;
    bottom: 0;
    left: 0;
    right: 0;
    height: 2px;
    background: var(--glow-line-gradient);
    opacity: 0.4;
  }

  .whats-new__inner {
    max-width: var(--max-width);
    margin-inline: auto;
    padding: 0 var(--space-lg);
  }

  .whats-new__header {
    display: flex;
    flex-direction: column;
    gap: var(--space-xs);
    margin-bottom: var(--space-2xl);
  }

  .whats-new__grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: var(--space-lg);
  }

  .whats-new__card {
    padding: var(--space-lg);
    border-radius: var(--radius-lg);
    border: 1px solid var(--border-subtle);
    background: var(--bg-card);
    transition: border-color var(--transition-fast), box-shadow var(--transition-base);
  }

  .whats-new__card:hover {
    border-color: var(--border-default);
    box-shadow: var(--glow-card-hover);
  }

  .whats-new__icon {
    width: 48px;
    height: 48px;
    border-radius: var(--radius-md);
    background: rgba(var(--accent-primary-rgb), 0.1);
    border: 1px solid rgba(var(--accent-primary-rgb), 0.2);
    display: flex;
    align-items: center;
    justify-content: center;
    color: var(--accent-primary);
    margin-bottom: var(--space-md);
  }

  .whats-new__title {
    font-size: var(--text-md);
    font-weight: 600;
    color: var(--text-primary);
    margin-bottom: var(--space-xs);
  }

  .whats-new__desc {
    font-size: var(--text-sm);
    color: var(--text-secondary);
    line-height: 1.6;
  }

  .whats-new__cta {
    margin-top: var(--space-xl);
    text-align: center;
  }

  /* ── Stats ── */
  .stats {
    position: relative;
    padding: var(--space-3xl) 0;
  }

  .stats__ambient {
    position: absolute;
    inset: 0;
    background:
      radial-gradient(ellipse 50% 80% at 25% 50%, rgba(var(--accent-primary-rgb), 0.1), transparent 70%),
      radial-gradient(ellipse 50% 80% at 75% 50%, rgba(var(--accent-secondary-rgb), 0.08), transparent 70%);
    pointer-events: none;
  }

  .stats__grid {
    position: relative;
    max-width: var(--max-width);
    margin-inline: auto;
    padding: 0 var(--space-lg);
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: var(--space-xl);
    text-align: center;
  }

  .stat-custom {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: var(--space-sm);
  }

  .stat-custom__number {
    font-size: clamp(36px, 5vw, 56px);
    font-weight: 700;
    background: var(--gradient-accent-text);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    filter: drop-shadow(0 0 20px rgba(var(--accent-primary-rgb), 0.3));
  }

  .stat-custom__rule {
    width: 24px;
    height: 2px;
    background: var(--gradient-accent-text);
    border-radius: 1px;
  }

  .stat-custom__label {
    font-family: var(--font-accent);
    font-size: var(--text-xs);
    font-weight: 600;
    letter-spacing: 3px;
    text-transform: uppercase;
    color: var(--text-ghost);
  }

  /* ── Features ── */
  .features {
    padding: 0 0 var(--space-4xl);
    position: relative;
  }

  .features::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 2px;
    background: var(--glow-line-gradient);
    opacity: 0.4;
  }

  .features__inner {
    max-width: var(--max-width);
    margin-inline: auto;
    padding: var(--space-4xl) var(--space-lg) 0;
  }

  .features__header {
    display: flex;
    flex-direction: column;
    gap: var(--space-xs);
    margin-bottom: var(--space-2xl);
  }

  .features__grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
    gap: var(--space-lg);
  }

  /* ── Showcase ── */
  .showcase-section {
    padding: var(--space-4xl) 0;
    background: var(--bg-surface);
    position: relative;
  }

  .showcase-section::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 2px;
    background: var(--glow-line-gradient);
    opacity: 0.4;
  }

  .showcase-section__inner {
    max-width: var(--max-width);
    margin-inline: auto;
    padding: 0 var(--space-lg);
  }

  .showcase-section__header {
    display: flex;
    flex-direction: column;
    gap: var(--space-xs);
    margin-bottom: var(--space-2xl);
  }

  .showcase-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(min(300px, 100%), 1fr));
    gap: var(--space-md);
  }

  .showcase-card {
    border: 1px solid var(--border-subtle);
    border-radius: var(--radius-lg);
    background: var(--bg-card);
    transition: border-color var(--transition-fast), box-shadow var(--transition-base);
    overflow: visible;
    min-width: 0;
    position: relative;
  }

  .showcase-card:focus-within {
    z-index: 10;
  }

  .showcase-card:hover {
    border-color: var(--border-default);
    box-shadow: var(--glow-card-hover);
  }

  .showcase-card__label {
    display: block;
    font-family: var(--font-accent);
    font-size: var(--text-xs);
    font-weight: 600;
    letter-spacing: 2px;
    text-transform: uppercase;
    color: var(--text-ghost);
    padding: var(--space-sm) var(--space-md);
    border-bottom: 1px solid var(--border-subtle);
    background: var(--bg-elevated);
    border-radius: var(--radius-lg) var(--radius-lg) 0 0;
  }

  .showcase-card__body {
    padding: var(--space-lg);
    display: flex;
    gap: var(--space-sm);
    flex-wrap: wrap;
    align-items: flex-start;
  }

  .showcase-card__body--stack {
    flex-direction: column;
  }

  .showcase-see-all {
    text-align: center;
    margin-top: var(--space-xl);
  }

  /* ── Code Section ── */
  .code-section {
    padding: var(--space-4xl) 0;
    position: relative;
  }

  .code-section::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 2px;
    background: var(--glow-line-gradient);
    opacity: 0.4;
  }

  .code-section__inner {
    max-width: var(--max-width);
    margin-inline: auto;
    padding: 0 var(--space-lg);
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--space-2xl);
    align-items: center;
  }

  .code-section__text {
    display: flex;
    flex-direction: column;
    gap: var(--space-xs);
    min-width: 0;
  }

  .code-section__features {
    display: flex;
    flex-direction: column;
    gap: var(--space-sm);
    margin-top: var(--space-lg);
  }

  .code-feature {
    display: flex;
    align-items: center;
    gap: var(--space-sm);
    font-size: var(--text-sm);
    color: var(--text-secondary);
  }

  .code-section__code {
    position: relative;
    min-width: 0;
  }

  .code-tabs {
    min-width: 0;
  }

  /* ── Frameworks Strip / Prism ── */
  .frameworks-strip {
    position: relative;
    padding: var(--space-3xl) 0;
    text-align: center;
    background:
      radial-gradient(ellipse 60% 80% at 50% 50%, rgba(var(--accent-primary-rgb), 0.06), transparent 70%);
  }

  .frameworks-strip::before,
  .frameworks-strip::after {
    content: '';
    position: absolute;
    left: 0;
    right: 0;
    height: 2px;
    background: var(--glow-line-gradient);
    opacity: 0.4;
  }

  .frameworks-strip::before { top: 0; }
  .frameworks-strip::after { bottom: 0; }

  .frameworks-strip__inner {
    max-width: var(--max-width);
    margin-inline: auto;
    padding: 0 var(--space-lg);
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: var(--space-sm);
  }

  .prism-visual {
    display: flex;
    align-items: center;
    gap: var(--space-xl);
    margin-top: var(--space-xl);
  }

  .prism-source {
    display: inline-flex;
    align-items: center;
    gap: var(--space-sm);
    padding: var(--space-sm) var(--space-lg);
    border-radius: var(--radius-md);
    border: 1px solid rgba(var(--accent-primary-rgb), 0.4);
    background: rgba(var(--accent-primary-rgb), 0.08);
    font-family: var(--font-mono);
    font-size: var(--text-sm);
    font-weight: 500;
    color: var(--text-primary);
    white-space: nowrap;
  }

  .prism-source svg {
    color: var(--accent-primary);
    flex-shrink: 0;
  }

  .prism-arrow {
    color: var(--text-ghost);
    flex-shrink: 0;
  }

  .prism-targets {
    display: grid;
    grid-template-columns: repeat(4, auto);
    gap: var(--space-sm);
  }

  .prism-target {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: var(--space-xs) var(--space-md);
    border-radius: var(--radius-md);
    border: 1px solid var(--border-subtle);
    background: var(--bg-elevated);
    font-size: var(--text-sm);
    font-weight: 500;
    color: var(--text-secondary);
    white-space: nowrap;
    transition: border-color var(--transition-fast), box-shadow var(--transition-fast), color var(--transition-fast);
  }

  .prism-target:hover {
    border-color: rgba(var(--accent-primary-rgb), 0.3);
    box-shadow: var(--glow-card-hover);
    color: var(--text-primary);
  }

  /* ── Responsive ── */
  @media (max-width: 900px) {
    .code-section__inner {
      grid-template-columns: 1fr;
    }
    .whats-new__grid {
      grid-template-columns: repeat(2, 1fr);
    }
    .prism-targets {
      grid-template-columns: repeat(3, auto);
    }
  }

  @media (max-width: 768px) {
    .hero {
      padding: calc(var(--space-2xl) + var(--nav-height, 64px)) var(--space-md) var(--space-xl);
      min-height: auto;
    }
    .hero__actions { flex-direction: column; align-items: center; }
    .whats-new { padding: var(--space-xl) 0; }
    .whats-new__grid { grid-template-columns: 1fr; }
    .stats { padding: var(--space-xl) 0; }
    .stats__grid { grid-template-columns: repeat(2, 1fr); gap: var(--space-lg); }
    .features { padding: var(--space-xl) 0; }
    .features__inner { padding-top: var(--space-xl); }
    .features__grid { grid-template-columns: 1fr; }
    .showcase-section { padding: var(--space-xl) 0; }
    .showcase-grid { grid-template-columns: 1fr; }
    .code-section { padding: var(--space-xl) 0; }
    .frameworks-strip { padding: var(--space-xl) 0; }
    .prism-visual {
      flex-direction: column;
    }
    .prism-arrow {
      transform: rotate(90deg);
    }
    .prism-targets {
      grid-template-columns: repeat(2, auto);
    }
  }

  @media (max-width: 480px) {
    .hero__headline-top { font-size: 28px; }
    .hero__headline-glow,
    .hero__headline-glow-shadow { font-size: 48px; }
    .stats__grid { grid-template-columns: repeat(2, 1fr); }
  }
</style>
