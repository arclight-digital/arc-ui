<div class="wordmark">
  <span class="wordmark__name">ARC UI</span>
  <arc-divider vertical variant="line-white" style="height: 20px;"></arc-divider>
  <span class="wordmark__expand"><span class="wordmark__letter">A</span>RC <span class="wordmark__letter">R</span>eactive <span class="wordmark__letter">C</span>omponents</span>
</div>

<style>
  .wordmark {
    display: flex;
    align-items: center;
    gap: var(--space-sm);
    line-height: 1;
    white-space: nowrap;
  }

  .wordmark__name {
    font-family: var(--font-body);
    font-size: var(--text-xl);
    font-weight: 600;
    letter-spacing: -0.5px;
    background: var(--gradient-accent-text);
    background-size: 200% 100%;
    background-position: 0% 50%;
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    filter: drop-shadow(0 0 10px rgba(var(--accent-primary-rgb), 0.15));
    transition:
      filter 400ms var(--ease-out-expo),
      background-position 600ms var(--ease-out-expo);
  }

  .wordmark__expand {
    font-family: var(--font-accent);
    font-size: var(--text-xs);
    font-weight: 500;
    letter-spacing: 1.5px;
    color: var(--text-ghost);
    transition: color 400ms var(--ease-out-expo);
  }

  .wordmark__letter {
    color: var(--accent-primary);
  }

  .wordmark:hover .wordmark__name {
    background-position: 100% 50%;
    filter:
      drop-shadow(0 0 16px rgba(var(--accent-primary-rgb), 0.5))
      drop-shadow(0 0 32px rgba(var(--accent-secondary-rgb), 0.2));
  }

  .wordmark:hover .wordmark__expand {
    color: var(--text-secondary);
  }

  @media (max-width: 768px) {
    .wordmark arc-divider { display: none; }
    .wordmark__expand { display: none; }
  }
</style>
